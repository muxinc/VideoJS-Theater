import { MediaProxyMixin } from "../../core/media/proxy.js";
import { MediaDelegateMixin } from "../../core/media/delegate.js";
import { CustomMediaMixin } from "./custom-media-element.js";
import Hls from "hls.js";

//#region src/dom/media/hls.ts
var HlsMediaDelegate = class {
	#engine = new Hls();
	attach(target) {
		this.#engine.attachMedia(target);
	}
	detach() {
		this.#engine.detachMedia();
	}
	set src(src) {
		this.#engine.loadSource(src);
	}
	get src() {
		return this.#engine.url ?? "";
	}
};
var HlsCustomMedia = class extends MediaDelegateMixin(CustomMediaMixin(globalThis.HTMLElement ?? class {}, { tag: "video" }), HlsMediaDelegate) {};
var HlsMedia = class extends MediaDelegateMixin(MediaProxyMixin(globalThis.HTMLVideoElement ?? class {}, globalThis.HTMLMediaElement ?? class {}, globalThis.EventTarget ?? class {}), HlsMediaDelegate) {};

//#endregion
export { HlsCustomMedia, HlsMedia, HlsMediaDelegate };
//# sourceMappingURL=hls.js.map