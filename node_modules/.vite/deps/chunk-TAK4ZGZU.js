import {
  require_react
} from "./chunk-SLS4EYW6.js";
import {
  __commonJS,
  __privateAdd,
  __privateGet,
  __privateMethod,
  __privateSet,
  __publicField,
  __toESM
} from "./chunk-4YJ6HKSX.js";

// node_modules/react/cjs/react-jsx-runtime.development.js
var require_react_jsx_runtime_development = __commonJS({
  "node_modules/react/cjs/react-jsx-runtime.development.js"(exports) {
    "use strict";
    (function() {
      function getComponentNameFromType(type) {
        if (null == type) return null;
        if ("function" === typeof type)
          return type.$$typeof === REACT_CLIENT_REFERENCE ? null : type.displayName || type.name || null;
        if ("string" === typeof type) return type;
        switch (type) {
          case REACT_FRAGMENT_TYPE:
            return "Fragment";
          case REACT_PROFILER_TYPE:
            return "Profiler";
          case REACT_STRICT_MODE_TYPE:
            return "StrictMode";
          case REACT_SUSPENSE_TYPE:
            return "Suspense";
          case REACT_SUSPENSE_LIST_TYPE:
            return "SuspenseList";
          case REACT_ACTIVITY_TYPE:
            return "Activity";
        }
        if ("object" === typeof type)
          switch ("number" === typeof type.tag && console.error(
            "Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue."
          ), type.$$typeof) {
            case REACT_PORTAL_TYPE:
              return "Portal";
            case REACT_CONTEXT_TYPE:
              return type.displayName || "Context";
            case REACT_CONSUMER_TYPE:
              return (type._context.displayName || "Context") + ".Consumer";
            case REACT_FORWARD_REF_TYPE:
              var innerType = type.render;
              type = type.displayName;
              type || (type = innerType.displayName || innerType.name || "", type = "" !== type ? "ForwardRef(" + type + ")" : "ForwardRef");
              return type;
            case REACT_MEMO_TYPE:
              return innerType = type.displayName || null, null !== innerType ? innerType : getComponentNameFromType(type.type) || "Memo";
            case REACT_LAZY_TYPE:
              innerType = type._payload;
              type = type._init;
              try {
                return getComponentNameFromType(type(innerType));
              } catch (x) {
              }
          }
        return null;
      }
      function testStringCoercion(value) {
        return "" + value;
      }
      function checkKeyStringCoercion(value) {
        try {
          testStringCoercion(value);
          var JSCompiler_inline_result = false;
        } catch (e) {
          JSCompiler_inline_result = true;
        }
        if (JSCompiler_inline_result) {
          JSCompiler_inline_result = console;
          var JSCompiler_temp_const = JSCompiler_inline_result.error;
          var JSCompiler_inline_result$jscomp$0 = "function" === typeof Symbol && Symbol.toStringTag && value[Symbol.toStringTag] || value.constructor.name || "Object";
          JSCompiler_temp_const.call(
            JSCompiler_inline_result,
            "The provided key is an unsupported type %s. This value must be coerced to a string before using it here.",
            JSCompiler_inline_result$jscomp$0
          );
          return testStringCoercion(value);
        }
      }
      function getTaskName(type) {
        if (type === REACT_FRAGMENT_TYPE) return "<>";
        if ("object" === typeof type && null !== type && type.$$typeof === REACT_LAZY_TYPE)
          return "<...>";
        try {
          var name = getComponentNameFromType(type);
          return name ? "<" + name + ">" : "<...>";
        } catch (x) {
          return "<...>";
        }
      }
      function getOwner() {
        var dispatcher = ReactSharedInternals.A;
        return null === dispatcher ? null : dispatcher.getOwner();
      }
      function UnknownOwner() {
        return Error("react-stack-top-frame");
      }
      function hasValidKey(config) {
        if (hasOwnProperty.call(config, "key")) {
          var getter = Object.getOwnPropertyDescriptor(config, "key").get;
          if (getter && getter.isReactWarning) return false;
        }
        return void 0 !== config.key;
      }
      function defineKeyPropWarningGetter(props, displayName) {
        function warnAboutAccessingKey() {
          specialPropKeyWarningShown || (specialPropKeyWarningShown = true, console.error(
            "%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://react.dev/link/special-props)",
            displayName
          ));
        }
        warnAboutAccessingKey.isReactWarning = true;
        Object.defineProperty(props, "key", {
          get: warnAboutAccessingKey,
          configurable: true
        });
      }
      function elementRefGetterWithDeprecationWarning() {
        var componentName = getComponentNameFromType(this.type);
        didWarnAboutElementRef[componentName] || (didWarnAboutElementRef[componentName] = true, console.error(
          "Accessing element.ref was removed in React 19. ref is now a regular prop. It will be removed from the JSX Element type in a future release."
        ));
        componentName = this.props.ref;
        return void 0 !== componentName ? componentName : null;
      }
      function ReactElement(type, key, props, owner, debugStack, debugTask) {
        var refProp = props.ref;
        type = {
          $$typeof: REACT_ELEMENT_TYPE,
          type,
          key,
          props,
          _owner: owner
        };
        null !== (void 0 !== refProp ? refProp : null) ? Object.defineProperty(type, "ref", {
          enumerable: false,
          get: elementRefGetterWithDeprecationWarning
        }) : Object.defineProperty(type, "ref", { enumerable: false, value: null });
        type._store = {};
        Object.defineProperty(type._store, "validated", {
          configurable: false,
          enumerable: false,
          writable: true,
          value: 0
        });
        Object.defineProperty(type, "_debugInfo", {
          configurable: false,
          enumerable: false,
          writable: true,
          value: null
        });
        Object.defineProperty(type, "_debugStack", {
          configurable: false,
          enumerable: false,
          writable: true,
          value: debugStack
        });
        Object.defineProperty(type, "_debugTask", {
          configurable: false,
          enumerable: false,
          writable: true,
          value: debugTask
        });
        Object.freeze && (Object.freeze(type.props), Object.freeze(type));
        return type;
      }
      function jsxDEVImpl(type, config, maybeKey, isStaticChildren, debugStack, debugTask) {
        var children = config.children;
        if (void 0 !== children)
          if (isStaticChildren)
            if (isArrayImpl(children)) {
              for (isStaticChildren = 0; isStaticChildren < children.length; isStaticChildren++)
                validateChildKeys(children[isStaticChildren]);
              Object.freeze && Object.freeze(children);
            } else
              console.error(
                "React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead."
              );
          else validateChildKeys(children);
        if (hasOwnProperty.call(config, "key")) {
          children = getComponentNameFromType(type);
          var keys = Object.keys(config).filter(function(k) {
            return "key" !== k;
          });
          isStaticChildren = 0 < keys.length ? "{key: someKey, " + keys.join(": ..., ") + ": ...}" : "{key: someKey}";
          didWarnAboutKeySpread[children + isStaticChildren] || (keys = 0 < keys.length ? "{" + keys.join(": ..., ") + ": ...}" : "{}", console.error(
            'A props object containing a "key" prop is being spread into JSX:\n  let props = %s;\n  <%s {...props} />\nReact keys must be passed directly to JSX without using spread:\n  let props = %s;\n  <%s key={someKey} {...props} />',
            isStaticChildren,
            children,
            keys,
            children
          ), didWarnAboutKeySpread[children + isStaticChildren] = true);
        }
        children = null;
        void 0 !== maybeKey && (checkKeyStringCoercion(maybeKey), children = "" + maybeKey);
        hasValidKey(config) && (checkKeyStringCoercion(config.key), children = "" + config.key);
        if ("key" in config) {
          maybeKey = {};
          for (var propName in config)
            "key" !== propName && (maybeKey[propName] = config[propName]);
        } else maybeKey = config;
        children && defineKeyPropWarningGetter(
          maybeKey,
          "function" === typeof type ? type.displayName || type.name || "Unknown" : type
        );
        return ReactElement(
          type,
          children,
          maybeKey,
          getOwner(),
          debugStack,
          debugTask
        );
      }
      function validateChildKeys(node) {
        isValidElement2(node) ? node._store && (node._store.validated = 1) : "object" === typeof node && null !== node && node.$$typeof === REACT_LAZY_TYPE && ("fulfilled" === node._payload.status ? isValidElement2(node._payload.value) && node._payload.value._store && (node._payload.value._store.validated = 1) : node._store && (node._store.validated = 1));
      }
      function isValidElement2(object) {
        return "object" === typeof object && null !== object && object.$$typeof === REACT_ELEMENT_TYPE;
      }
      var React = require_react(), REACT_ELEMENT_TYPE = Symbol.for("react.transitional.element"), REACT_PORTAL_TYPE = Symbol.for("react.portal"), REACT_FRAGMENT_TYPE = Symbol.for("react.fragment"), REACT_STRICT_MODE_TYPE = Symbol.for("react.strict_mode"), REACT_PROFILER_TYPE = Symbol.for("react.profiler"), REACT_CONSUMER_TYPE = Symbol.for("react.consumer"), REACT_CONTEXT_TYPE = Symbol.for("react.context"), REACT_FORWARD_REF_TYPE = Symbol.for("react.forward_ref"), REACT_SUSPENSE_TYPE = Symbol.for("react.suspense"), REACT_SUSPENSE_LIST_TYPE = Symbol.for("react.suspense_list"), REACT_MEMO_TYPE = Symbol.for("react.memo"), REACT_LAZY_TYPE = Symbol.for("react.lazy"), REACT_ACTIVITY_TYPE = Symbol.for("react.activity"), REACT_CLIENT_REFERENCE = Symbol.for("react.client.reference"), ReactSharedInternals = React.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, hasOwnProperty = Object.prototype.hasOwnProperty, isArrayImpl = Array.isArray, createTask = console.createTask ? console.createTask : function() {
        return null;
      };
      React = {
        react_stack_bottom_frame: function(callStackForError) {
          return callStackForError();
        }
      };
      var specialPropKeyWarningShown;
      var didWarnAboutElementRef = {};
      var unknownOwnerDebugStack = React.react_stack_bottom_frame.bind(
        React,
        UnknownOwner
      )();
      var unknownOwnerDebugTask = createTask(getTaskName(UnknownOwner));
      var didWarnAboutKeySpread = {};
      exports.Fragment = REACT_FRAGMENT_TYPE;
      exports.jsx = function(type, config, maybeKey) {
        var trackActualOwner = 1e4 > ReactSharedInternals.recentlyCreatedOwnerStacks++;
        return jsxDEVImpl(
          type,
          config,
          maybeKey,
          false,
          trackActualOwner ? Error("react-stack-top-frame") : unknownOwnerDebugStack,
          trackActualOwner ? createTask(getTaskName(type)) : unknownOwnerDebugTask
        );
      };
      exports.jsxs = function(type, config, maybeKey) {
        var trackActualOwner = 1e4 > ReactSharedInternals.recentlyCreatedOwnerStacks++;
        return jsxDEVImpl(
          type,
          config,
          maybeKey,
          true,
          trackActualOwner ? Error("react-stack-top-frame") : unknownOwnerDebugStack,
          trackActualOwner ? createTask(getTaskName(type)) : unknownOwnerDebugTask
        );
      };
    })();
  }
});

// node_modules/react/jsx-runtime.js
var require_jsx_runtime = __commonJS({
  "node_modules/react/jsx-runtime.js"(exports, module) {
    "use strict";
    if (false) {
      module.exports = null;
    } else {
      module.exports = require_react_jsx_runtime_development();
    }
  }
});

// node_modules/@videojs/store/dist/dev/core/shallow-equal.js
var hasOwn = Object.prototype.hasOwnProperty;
function shallowEqual(a, b) {
  if (Object.is(a, b)) return true;
  if (typeof a !== "object" || a === null || typeof b !== "object" || b === null) return false;
  const keysA = Object.keys(a);
  const keysB = Object.keys(b);
  if (keysA.length !== keysB.length) return false;
  for (const key of keysA) if (!hasOwn.call(b, key) || !Object.is(a[key], b[key])) return false;
  return true;
}

// node_modules/@videojs/store/dist/dev/react/hooks/use-selector.js
var import_react = __toESM(require_react(), 1);
function useSelector(subscribe, getSnapshot, selector, isEqual = shallowEqual) {
  const cache = (0, import_react.useRef)(void 0);
  const getSelectedSnapshot = () => {
    const next = selector(getSnapshot());
    if (cache.current !== void 0 && isEqual(cache.current, next)) return cache.current;
    cache.current = next;
    return next;
  };
  return (0, import_react.useSyncExternalStore)(subscribe, getSelectedSnapshot, getSelectedSnapshot);
}

// node_modules/@videojs/utils/dist/predicate/predicate.js
function isString(value) {
  return typeof value === "string";
}
function isNumber(value) {
  return typeof value === "number";
}
function isFunction(value) {
  return typeof value === "function";
}
function isNull(value) {
  return value === null;
}
function isUndefined(value) {
  return typeof value === "undefined";
}
function isObject(value) {
  return value !== null && typeof value === "object";
}
function isPlainObject(value) {
  if (!isObject(value)) return false;
  const proto = Object.getPrototypeOf(value);
  return proto === null || proto === Object.prototype;
}

// node_modules/@videojs/utils/dist/function/identity.js
function identity(value) {
  return value;
}

// node_modules/@videojs/utils/dist/function/noop.js
function noop(..._args) {
}

// node_modules/@videojs/utils/dist/function/throttle.js
function throttle(fn, ms) {
  let timerId = null;
  let latestArgs;
  const throttled = (...args) => {
    latestArgs = args;
    if (timerId !== null) return;
    timerId = setTimeout(() => {
      timerId = null;
      fn(...latestArgs);
    }, ms);
  };
  throttled.cancel = () => {
    if (timerId !== null) {
      clearTimeout(timerId);
      timerId = null;
    }
  };
  return throttled;
}

// node_modules/@videojs/store/dist/dev/react/hooks/use-store.js
var noopSubscribe = () => noop;
function useStore(store, selector, isEqual) {
  return useSelector(selector ? (cb) => store.subscribe(cb) : noopSubscribe, selector ? () => store.state : () => store, selector ?? identity, isEqual);
}

// node_modules/@videojs/store/dist/dev/react/hooks/use-snapshot.js
function useSnapshot(state, selector, isEqual) {
  return useSelector((cb) => state.subscribe(cb), () => state.current, selector ?? identity, isEqual);
}

// node_modules/@videojs/react/dist/dev/utils/use-composed-refs.js
var import_react2 = __toESM(require_react(), 1);
function setRef(ref, value) {
  if (isFunction(ref)) return ref(value);
  else if (ref !== null && ref !== void 0) ref.current = value;
}
function composeRefs(...refs) {
  const flatRefs = refs.flat();
  return (node) => {
    const cleanups = flatRefs.map((ref) => setRef(ref, node));
    if (cleanups.some(isFunction)) return () => {
      for (let i = 0; i < cleanups.length; i++) {
        const cleanup = cleanups[i];
        if (isFunction(cleanup)) cleanup();
        else setRef(flatRefs[i], null);
      }
    };
  };
}
function useComposedRefs(...refs) {
  return (0, import_react2.useCallback)(composeRefs(...refs), [...refs]);
}

// node_modules/@videojs/react/dist/dev/player/context.js
var import_react4 = __toESM(require_react(), 1);
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var PlayerContext = (0, import_react4.createContext)(null);
var EMPTY_UNSUBSCRIBE = () => {
};
var EMPTY_STORE = {
  state: {},
  subscribe: () => EMPTY_UNSUBSCRIBE
};
function PlayerContextProvider({ value, children }) {
  return (0, import_jsx_runtime.jsx)(PlayerContext.Provider, {
    value,
    children
  });
}
function usePlayerContext() {
  const ctx = (0, import_react4.useContext)(PlayerContext);
  if (!ctx) throw new Error("usePlayerContext must be used within a Player Provider");
  return ctx;
}
function usePlayer(selector) {
  const { store } = usePlayerContext();
  return useStore(store, selector);
}
function useOptionalPlayer(selector) {
  const ctx = (0, import_react4.useContext)(PlayerContext);
  const value = useStore((ctx == null ? void 0 : ctx.store) ?? EMPTY_STORE, ctx ? selector : void 0);
  return ctx ? value : void 0;
}
function useMedia() {
  const { media } = usePlayerContext();
  return media;
}
function useMediaRegistration() {
  var _a17;
  return (_a17 = (0, import_react4.useContext)(PlayerContext)) == null ? void 0 : _a17.setMedia;
}
var Container = (0, import_react4.forwardRef)(function Container2({ children, ...props }, ref) {
  const { store, media } = usePlayerContext();
  const internalRef = (0, import_react4.useRef)(null);
  const composedRef = useComposedRefs(ref, internalRef);
  (0, import_react4.useEffect)(() => {
    if (!media) return;
    return store.attach({
      media,
      container: internalRef.current
    });
  }, [media, store]);
  return (0, import_jsx_runtime.jsx)("div", {
    ref: composedRef,
    ...props,
    children
  });
});

// node_modules/@videojs/store/dist/dev/core/abort-controller-registry.js
var _base, _keys, _a;
var AbortControllerRegistry = (_a = class {
  constructor() {
    __privateAdd(this, _base, new AbortController());
    __privateAdd(this, _keys, /* @__PURE__ */ new Map());
  }
  /** The attach-scoped signal. Aborts on detach or reattach. */
  get base() {
    return __privateGet(this, _base).signal;
  }
  /** Clears all keyed signals, leaving base intact. */
  clear() {
    for (const controller of __privateGet(this, _keys).values()) controller.abort();
    __privateGet(this, _keys).clear();
  }
  /** Resets base and clears all keyed signals. */
  reset() {
    this.clear();
    __privateGet(this, _base).abort();
    __privateSet(this, _base, new AbortController());
  }
  /** Creates a new signal for the key, superseding any previous signal. */
  supersede(key) {
    var _a17;
    (_a17 = __privateGet(this, _keys).get(key)) == null ? void 0 : _a17.abort();
    const controller = new AbortController();
    __privateGet(this, _keys).set(key, controller);
    return AbortSignal.any([__privateGet(this, _base).signal, controller.signal]);
  }
}, _base = new WeakMap(), _keys = new WeakMap(), _a);

// node_modules/@videojs/store/dist/dev/core/errors.js
var StoreError = class extends Error {
  constructor(code, options) {
    super((options == null ? void 0 : options.message) ?? code);
    __publicField(this, "code");
    __publicField(this, "cause");
    this.name = "StoreError";
    this.code = code;
    this.cause = options == null ? void 0 : options.cause;
  }
};
function throwNoTargetError() {
  throw new StoreError("NO_TARGET");
}
function throwDestroyedError() {
  throw new StoreError("DESTROYED");
}

// node_modules/@videojs/utils/dist/object/defaults.js
function defaults(object, defaultValues) {
  const result = { ...defaultValues };
  for (const key in object) if (!isUndefined(object[key])) result[key] = object[key];
  return result;
}

// node_modules/@videojs/utils/dist/object/pick.js
function pick(obj, keys) {
  const result = {};
  for (const key of keys) result[key] = obj[key];
  return result;
}

// node_modules/@videojs/store/dist/dev/core/selector.js
var stateContext = {
  target: throwNoTargetError,
  signals: new AbortControllerRegistry()
};
function createSelector(slice) {
  const initialState = slice.state(stateContext);
  const keys = Object.keys(initialState);
  const firstKey = keys[0];
  if (!firstKey) return Object.assign(() => void 0, { displayName: slice.name });
  return Object.assign((state) => {
    if (!(firstKey in state)) return void 0;
    return pick(state, keys);
  }, { displayName: slice.name });
}

// node_modules/@videojs/store/dist/dev/core/combine.js
function combine(...slices) {
  return {
    state: (ctx) => {
      const states = slices.map((slice) => slice.state(ctx));
      return Object.assign({}, ...states);
    },
    attach: (ctx) => {
      var _a17;
      for (const slice of slices) try {
        (_a17 = slice.attach) == null ? void 0 : _a17.call(slice, ctx);
      } catch (err) {
        ctx.reportError(err);
      }
    }
  };
}

// node_modules/@videojs/store/dist/dev/core/slice.js
function defineSlice() {
  return (config) => config;
}

// node_modules/@videojs/store/dist/dev/core/state.js
var isFlushScheduled = false;
function scheduleFlush() {
  if (isFlushScheduled) return;
  isFlushScheduled = true;
  queueMicrotask(flush);
}
var pendingContainers = /* @__PURE__ */ new Set();
function flush() {
  isFlushScheduled = false;
  for (const container of pendingContainers) container.flush();
  pendingContainers.clear();
}
var hasOwnProp = Object.prototype.hasOwnProperty;
var _current, _listeners, _pending, _StateContainer_instances, markPending_fn, _a2;
var StateContainer = (_a2 = class {
  constructor(initial) {
    __privateAdd(this, _StateContainer_instances);
    __privateAdd(this, _current);
    __privateAdd(this, _listeners, /* @__PURE__ */ new Set());
    __privateAdd(this, _pending, false);
    __privateSet(this, _current, Object.freeze({ ...initial }));
  }
  get current() {
    return __privateGet(this, _current);
  }
  patch(partial) {
    const next = { ...__privateGet(this, _current) };
    let changed = false;
    for (const key in partial) {
      if (!hasOwnProp.call(partial, key)) continue;
      const value = partial[key];
      if (!Object.is(__privateGet(this, _current)[key], value)) {
        next[key] = value;
        changed = true;
      }
    }
    if (changed) {
      __privateSet(this, _current, Object.freeze(next));
      __privateMethod(this, _StateContainer_instances, markPending_fn).call(this);
    }
  }
  subscribe(callback, options) {
    const signal = options == null ? void 0 : options.signal;
    if (signal == null ? void 0 : signal.aborted) return noop;
    __privateGet(this, _listeners).add(callback);
    if (!signal) return () => __privateGet(this, _listeners).delete(callback);
    const onAbort = () => __privateGet(this, _listeners).delete(callback);
    signal.addEventListener("abort", onAbort, { once: true });
    return () => {
      signal.removeEventListener("abort", onAbort);
      __privateGet(this, _listeners).delete(callback);
    };
  }
  flush() {
    if (!__privateGet(this, _pending)) return;
    __privateSet(this, _pending, false);
    for (const fn of __privateGet(this, _listeners)) fn();
  }
}, _current = new WeakMap(), _listeners = new WeakMap(), _pending = new WeakMap(), _StateContainer_instances = new WeakSet(), markPending_fn = function() {
  __privateSet(this, _pending, true);
  pendingContainers.add(this);
  scheduleFlush();
}, _a2);
function createState(initial) {
  return new StateContainer(initial);
}

// node_modules/@videojs/store/dist/dev/core/store.js
var STORE_SYMBOL = Symbol("@videojs/store");
function createStore() {
  return (slice, options = {}) => {
    var _a17;
    let target = null;
    let destroyed = false;
    const setupAbort = new AbortController();
    const signals = new AbortControllerRegistry();
    let state;
    function validate() {
      if (destroyed) throwDestroyedError();
      if (!target) throwNoTargetError();
    }
    const initialState = slice.state({
      target: () => {
        validate();
        return target;
      },
      signals
    });
    state = createState(initialState);
    const store = {
      [STORE_SYMBOL]: true,
      get $state() {
        return state;
      },
      get target() {
        return target;
      },
      get destroyed() {
        return destroyed;
      },
      get state() {
        return state.current;
      },
      attach,
      destroy,
      subscribe
    };
    for (const key of Object.keys(initialState)) Object.defineProperty(store, key, {
      get: () => state.current[key],
      enumerable: true
    });
    try {
      (_a17 = options.onSetup) == null ? void 0 : _a17.call(options, {
        store,
        signal: setupAbort.signal
      });
    } catch (error) {
      reportError(error);
    }
    return store;
    function attach(newTarget) {
      var _a18, _b;
      if (destroyed) throwDestroyedError();
      signals.reset();
      target = newTarget;
      const attachContext = {
        target: newTarget,
        signal: signals.base,
        get: () => state.current,
        set: (partial) => state.patch(partial),
        reportError,
        store: {
          get state() {
            return state.current;
          },
          subscribe
        }
      };
      try {
        (_a18 = slice.attach) == null ? void 0 : _a18.call(slice, attachContext);
      } catch (error) {
        reportError(error);
      }
      try {
        (_b = options.onAttach) == null ? void 0 : _b.call(options, {
          store,
          target: newTarget,
          signal: signals.base
        });
      } catch (error) {
        reportError(error);
      }
      return detach;
    }
    function detach() {
      if (isNull(target)) return;
      signals.reset();
      target = null;
      state.patch(initialState);
    }
    function destroy() {
      if (destroyed) return;
      destroyed = true;
      detach();
      setupAbort.abort();
    }
    function subscribe(callback, options2) {
      return state.subscribe(callback, options2);
    }
    function reportError(error) {
      if (options.onError) options.onError({
        store,
        error
      });
      else console.error("[vjs-store]", error);
    }
  };
}

// node_modules/@videojs/react/dist/dev/utils/merge-props.js
function isEventHandlerKey(key) {
  return key.charCodeAt(0) === 111 && key.charCodeAt(1) === 110 && key.charCodeAt(2) >= 65 && key.charCodeAt(2) <= 90;
}
function isEventHandler(key, value) {
  return isEventHandlerKey(key) && (typeof value === "function" || typeof value === "undefined");
}
function mergeEventHandlers(ours, theirs) {
  if (!theirs) return ours;
  if (!ours) return theirs;
  return (event) => {
    theirs(event);
    ours(event);
  };
}
function mergeClassNames(ours, theirs) {
  if (theirs && ours) return `${theirs} ${ours}`;
  return theirs || ours;
}
function mergeStyles(ours, theirs) {
  if (!theirs) return ours;
  if (!ours) return theirs;
  return {
    ...ours,
    ...theirs
  };
}
function mergeOne(merged, props) {
  if (!props) return merged;
  for (const key in props) {
    const value = props[key];
    if (key === "className") merged.className = mergeClassNames(merged.className, value);
    else if (key === "style") merged.style = mergeStyles(merged.style, value);
    else if (isEventHandler(key, value)) merged[key] = mergeEventHandlers(merged[key], value);
    else merged[key] = value;
  }
  return merged;
}
function mergeProps(...propSets) {
  let merged = {};
  for (const props of propSets) merged = mergeOne(merged, props);
  return merged;
}

// node_modules/@videojs/core/dist/dev/dom/feature.js
var definePlayerFeature = defineSlice();

// node_modules/@videojs/utils/dist/dom/direction.js
function isRTL(element) {
  var _a17;
  const dir = (_a17 = element.closest("[dir]")) == null ? void 0 : _a17.getAttribute("dir");
  if (dir) return dir.toLowerCase() === "rtl";
  return getComputedStyle(element).direction === "rtl";
}

// node_modules/@videojs/utils/dist/dom/event.js
function onEvent(target, type, options) {
  return new Promise((resolve, reject) => {
    var _a17, _b;
    const handleAbort = () => {
      var _a18;
      reject(((_a18 = options == null ? void 0 : options.signal) == null ? void 0 : _a18.reason) ?? "Aborted");
    };
    if ((_a17 = options == null ? void 0 : options.signal) == null ? void 0 : _a17.aborted) {
      handleAbort();
      return;
    }
    (_b = options == null ? void 0 : options.signal) == null ? void 0 : _b.addEventListener("abort", handleAbort, { once: true });
    target.addEventListener(type, (event) => {
      var _a18;
      (_a18 = options == null ? void 0 : options.signal) == null ? void 0 : _a18.removeEventListener("abort", handleAbort);
      resolve(event);
    }, {
      ...options,
      once: true
    });
  });
}

// node_modules/@videojs/utils/dist/dom/supports.js
function supportsAnchorPositioning() {
  return typeof CSS !== "undefined" && CSS.supports("anchor-name: --a");
}

// node_modules/@videojs/utils/dist/dom/listen.js
function listen(target, type, listener, options) {
  target.addEventListener(type, listener, options);
  return () => target.removeEventListener(type, listener, options);
}

// node_modules/@videojs/utils/dist/dom/text-track.js
function findTrackElement(media, track) {
  for (const el of media.querySelectorAll("track")) if (el.track === track) return el;
  return null;
}

// node_modules/@videojs/utils/dist/dom/time-ranges.js
function serializeTimeRanges(ranges) {
  const result = [];
  for (let i = 0; i < ranges.length; i++) result.push([ranges.start(i), ranges.end(i)]);
  return result;
}

// node_modules/@videojs/core/dist/dev/dom/store/features/buffer.js
var bufferFeature = definePlayerFeature({
  name: "buffer",
  state: () => ({
    buffered: [],
    seekable: []
  }),
  attach({ target, signal, set }) {
    const { media } = target;
    const sync = () => set({
      buffered: serializeTimeRanges(media.buffered),
      seekable: serializeTimeRanges(media.seekable)
    });
    sync();
    listen(media, "progress", sync, { signal });
    listen(media, "emptied", sync, { signal });
  }
});

// node_modules/@videojs/core/dist/dev/dom/store/features/controls.js
var IDLE_DELAY = 2e3;
var TAP_THRESHOLD = 250;
var controlsFeature = definePlayerFeature({
  name: "controls",
  state: () => ({
    userActive: true,
    controlsVisible: true
  }),
  attach({ target, signal, get, set }) {
    const { media, container } = target;
    if (isNull(container)) {
      console.warn("[vjs] controlsFeature requires a container element for activity tracking.");
      return;
    }
    function computeVisible(userActive) {
      return userActive || media.paused;
    }
    let idleTimer;
    function clearIdle() {
      clearTimeout(idleTimer);
      idleTimer = void 0;
    }
    function scheduleIdle() {
      clearIdle();
      idleTimer = setTimeout(setInactive, IDLE_DELAY);
    }
    function setActive() {
      if (!get().userActive) set({
        userActive: true,
        controlsVisible: true
      });
      scheduleIdle();
    }
    function setInactive() {
      clearIdle();
      set({
        userActive: false,
        controlsVisible: computeVisible(false)
      });
    }
    let pointerDownTime = 0;
    function onPointerDown() {
      pointerDownTime = Date.now();
    }
    function onPointerUp(event) {
      if (event.pointerType === "touch" && Date.now() - pointerDownTime < TAP_THRESHOLD) if (get().controlsVisible) {
        clearIdle();
        set({
          userActive: false,
          controlsVisible: computeVisible(false)
        });
      } else setActive();
      else setActive();
    }
    function onPlaybackChange() {
      const { userActive } = get();
      set({ controlsVisible: computeVisible(userActive) });
      if (!media.paused && userActive) scheduleIdle();
    }
    listen(container, "pointermove", setActive, { signal });
    listen(container, "pointerdown", onPointerDown, { signal });
    listen(container, "pointerup", onPointerUp, { signal });
    listen(container, "keyup", setActive, { signal });
    listen(container, "focusin", setActive, { signal });
    listen(container, "pointerleave", setInactive, { signal });
    listen(media, "play", onPlaybackChange, { signal });
    listen(media, "pause", onPlaybackChange, { signal });
    listen(media, "ended", onPlaybackChange, { signal });
    signal.addEventListener("abort", clearIdle, { once: true });
    scheduleIdle();
  }
});

// node_modules/@videojs/core/dist/dev/dom/presentation/fullscreen.js
function isFullscreenSupported() {
  const doc = document;
  if (doc.fullscreenEnabled || doc.webkitFullscreenEnabled) return true;
  return document.createElement("video").webkitSupportsFullscreen === true;
}
function getFullscreenElement() {
  const doc = document;
  return doc.fullscreenElement ?? doc.webkitFullscreenElement ?? null;
}
function isElementFullscreen(container, media) {
  const video = media;
  if (video.webkitDisplayingFullscreen && video.webkitPresentationMode === "fullscreen") return true;
  const target = container ?? media;
  if (getFullscreenElement() === target) return true;
  try {
    return target.matches(":fullscreen");
  } catch {
    return false;
  }
}
async function enterFullscreen(container, media) {
  const video = media;
  if (container) {
    const el = container;
    if (isFunction(el.requestFullscreen)) return el.requestFullscreen();
    if (isFunction(el.webkitRequestFullscreen)) return el.webkitRequestFullscreen();
    if (isFunction(el.webkitRequestFullScreen)) return el.webkitRequestFullScreen();
  }
  if (isFunction(video.webkitEnterFullscreen)) {
    video.webkitEnterFullscreen();
    return;
  }
  if (isFunction(media.requestFullscreen)) return media.requestFullscreen();
  throw new DOMException("Fullscreen not supported", "NotSupportedError");
}
async function exitFullscreen() {
  const doc = document;
  const video = getFullscreenElement();
  if (isFunction(doc.exitFullscreen)) return doc.exitFullscreen();
  if (isFunction(doc.webkitExitFullscreen)) return doc.webkitExitFullscreen();
  if (isFunction(doc.webkitCancelFullScreen)) return doc.webkitCancelFullScreen();
  if (video && isFunction(video.webkitExitFullscreen)) {
    video.webkitExitFullscreen();
    return;
  }
}

// node_modules/@videojs/core/dist/dev/dom/presentation/pip.js
function isPiPSupported() {
  if (document.pictureInPictureEnabled) {
    const isSafari = /.*Version\/.*Safari\/.*/.test(navigator.userAgent);
    const isPWA = typeof matchMedia === "function" && matchMedia("(display-mode: standalone)").matches;
    return !isSafari || !isPWA;
  }
  return isFunction(document.createElement("video").webkitSetPresentationMode);
}
function isPiPActive(media) {
  if (document.pictureInPictureElement === media) return true;
  return media.webkitPresentationMode === "picture-in-picture";
}
async function enterPiP(media) {
  const video = media;
  if (isFunction(video.requestPictureInPicture)) {
    await video.requestPictureInPicture();
    return;
  }
  if (isFunction(video.webkitSetPresentationMode)) {
    video.webkitSetPresentationMode("picture-in-picture");
    return;
  }
  throw new DOMException("Picture-in-Picture not supported", "NotSupportedError");
}
async function exitPiP(media) {
  if (document.pictureInPictureElement && isFunction(document.exitPictureInPicture)) {
    await document.exitPictureInPicture();
    return;
  }
  if (media) {
    const video = media;
    if (video.webkitPresentationMode === "picture-in-picture" && isFunction(video.webkitSetPresentationMode)) {
      video.webkitSetPresentationMode("inline");
      return;
    }
  }
}

// node_modules/@videojs/core/dist/dev/dom/store/features/fullscreen.js
var fullscreenFeature = definePlayerFeature({
  name: "fullscreen",
  state: ({ target }) => ({
    fullscreen: false,
    fullscreenAvailability: "unavailable",
    async requestFullscreen() {
      const { media, container } = target();
      if (isPiPActive(media)) await exitPiP(media);
      return enterFullscreen(container, media);
    },
    async exitFullscreen() {
      return exitFullscreen();
    }
  }),
  attach({ target, signal, set }) {
    const { media, container } = target;
    set({ fullscreenAvailability: isFullscreenSupported() ? "available" : "unsupported" });
    const sync = () => set({ fullscreen: isElementFullscreen(container, media) });
    sync();
    listen(document, "fullscreenchange", sync, { signal });
    listen(document, "webkitfullscreenchange", sync, { signal });
    if ("webkitPresentationMode" in media) listen(media, "webkitpresentationmodechanged", sync, { signal });
  }
});

// node_modules/@videojs/core/dist/dev/dom/store/features/pip.js
var pipFeature = definePlayerFeature({
  name: "pip",
  state: ({ target }) => ({
    pip: false,
    pipAvailability: "unavailable",
    async requestPiP() {
      const { media, container } = target();
      if (isElementFullscreen(container, media)) await exitFullscreen();
      return enterPiP(media);
    },
    async exitPiP() {
      const { media } = target();
      return exitPiP(media);
    }
  }),
  attach({ target, signal, set }) {
    const { media } = target;
    set({ pipAvailability: isPiPSupported() ? "available" : "unsupported" });
    const sync = () => set({ pip: isPiPActive(media) });
    sync();
    listen(media, "enterpictureinpicture", sync, { signal });
    listen(media, "leavepictureinpicture", sync, { signal });
    if ("webkitPresentationMode" in media) listen(media, "webkitpresentationmodechanged", sync, { signal });
  }
});

// node_modules/@videojs/core/dist/dev/dom/store/features/playback.js
var playbackFeature = definePlayerFeature({
  name: "playback",
  state: ({ target }) => ({
    paused: true,
    ended: false,
    started: false,
    waiting: false,
    play() {
      return target().media.play();
    },
    pause() {
      target().media.pause();
    }
  }),
  attach({ target, signal, set }) {
    const { media } = target;
    const sync = () => set({
      paused: media.paused,
      ended: media.ended,
      started: !media.paused || media.currentTime > 0,
      waiting: media.readyState < HTMLMediaElement.HAVE_FUTURE_DATA && !media.paused
    });
    sync();
    listen(media, "play", sync, { signal });
    listen(media, "pause", sync, { signal });
    listen(media, "ended", sync, { signal });
    listen(media, "playing", sync, { signal });
    listen(media, "waiting", sync, { signal });
  }
});

// node_modules/@videojs/core/dist/dev/dom/store/features/playback-rate.js
var DEFAULT_RATES = [
  1,
  1.2,
  1.5,
  1.7,
  2
];
var playbackRateFeature = definePlayerFeature({
  name: "playbackRate",
  state: ({ target }) => ({
    playbackRates: DEFAULT_RATES,
    playbackRate: 1,
    setPlaybackRate(rate) {
      target().media.playbackRate = rate;
    }
  }),
  attach({ target, signal, set }) {
    const { media } = target;
    const sync = () => set({ playbackRate: media.playbackRate });
    sync();
    listen(media, "ratechange", sync, { signal });
  }
});

// node_modules/@videojs/core/dist/dev/dom/store/features/source.js
var sourceFeature = definePlayerFeature({
  name: "source",
  state: ({ target, signals }) => ({
    source: null,
    canPlay: false,
    loadSource(src) {
      signals.clear();
      const { media } = target();
      media.src = src;
      media.load();
      return src;
    }
  }),
  attach({ target, signal, set }) {
    const { media } = target;
    const sync = () => set({
      source: media.currentSrc || media.src || null,
      canPlay: media.readyState >= HTMLMediaElement.HAVE_ENOUGH_DATA
    });
    sync();
    listen(media, "canplay", sync, { signal });
    listen(media, "canplaythrough", sync, { signal });
    listen(media, "loadstart", sync, { signal });
    listen(media, "emptied", sync, { signal });
  }
});

// node_modules/@videojs/core/dist/dev/dom/store/features/text-track.js
var textTrackFeature = definePlayerFeature({
  name: "textTrack",
  state: () => ({
    chaptersCues: [],
    thumbnailCues: [],
    thumbnailTrackSrc: null
  }),
  attach({ target, signal, set }) {
    const { media } = target;
    let trackCleanup = null;
    function sync() {
      var _a17, _b, _c;
      trackCleanup == null ? void 0 : trackCleanup.abort();
      trackCleanup = new AbortController();
      let chaptersTrack = null;
      let thumbnailTrack = null;
      for (let i = 0; i < media.textTracks.length; i++) {
        const track = media.textTracks[i];
        if (!chaptersTrack && track.kind === "chapters") chaptersTrack = track;
        if (!thumbnailTrack && track.kind === "metadata" && track.label === "thumbnails") thumbnailTrack = track;
      }
      const chaptersCues = (chaptersTrack == null ? void 0 : chaptersTrack.cues) ? Array.from(chaptersTrack.cues) : [];
      const thumbnailCues = (thumbnailTrack == null ? void 0 : thumbnailTrack.cues) ? Array.from(thumbnailTrack.cues) : [];
      let thumbnailTrackSrc = null;
      if (thumbnailTrack) thumbnailTrackSrc = ((_a17 = findTrackElement(media, thumbnailTrack)) == null ? void 0 : _a17.src) ?? null;
      for (const trackEl of media.querySelectorAll("track")) if (!((_c = (_b = trackEl.track) == null ? void 0 : _b.cues) == null ? void 0 : _c.length)) listen(trackEl, "load", sync, { signal: trackCleanup.signal });
      set({
        chaptersCues,
        thumbnailCues,
        thumbnailTrackSrc
      });
    }
    sync();
    listen(media.textTracks, "addtrack", sync, { signal });
    listen(media.textTracks, "removetrack", sync, { signal });
    listen(media.textTracks, "change", sync, { signal });
    listen(media, "loadstart", sync, { signal });
    signal.addEventListener("abort", () => trackCleanup == null ? void 0 : trackCleanup.abort(), { once: true });
  }
});

// node_modules/@videojs/core/dist/dev/dom/media/predicate.js
function hasMetadata(media) {
  return media.readyState >= HTMLMediaElement.HAVE_METADATA;
}

// node_modules/@videojs/core/dist/dev/dom/store/signal-keys.js
var signalKeys = { seek: Symbol.for("@videojs/seek") };

// node_modules/@videojs/core/dist/dev/dom/store/features/time.js
var timeFeature = definePlayerFeature({
  name: "time",
  state: ({ target, signals }) => ({
    currentTime: 0,
    duration: 0,
    seeking: false,
    async seek(time) {
      const { media } = target(), signal = signals.supersede(signalKeys.seek);
      if (!hasMetadata(media)) {
        if (!await onEvent(media, "loadedmetadata", { signal }).catch(() => false)) return media.currentTime;
      }
      media.currentTime = Math.max(0, Math.min(time, media.duration || Infinity));
      await onEvent(media, "seeked", { signal }).catch(noop);
      return media.currentTime;
    }
  }),
  attach({ target, signal, set }) {
    const { media } = target;
    const sync = () => set({
      currentTime: media.currentTime,
      duration: Number.isFinite(media.duration) ? media.duration : 0,
      seeking: media.seeking
    });
    sync();
    listen(media, "timeupdate", sync, { signal });
    listen(media, "durationchange", sync, { signal });
    listen(media, "seeking", sync, { signal });
    listen(media, "seeked", sync, { signal });
    listen(media, "loadedmetadata", sync, { signal });
    listen(media, "emptied", sync, { signal });
  }
});

// node_modules/@videojs/core/dist/dev/dom/store/features/volume.js
var volumeFeature = definePlayerFeature({
  name: "volume",
  state: ({ target }) => ({
    volume: 1,
    muted: false,
    volumeAvailability: "unavailable",
    changeVolume(volume) {
      const { media } = target();
      media.volume = Math.max(0, Math.min(1, volume));
      return media.volume;
    },
    toggleMute() {
      const { media } = target();
      media.muted = !media.muted;
      return media.muted;
    }
  }),
  attach({ target, signal, set }) {
    const { media } = target;
    set({ volumeAvailability: canSetVolume() });
    const sync = () => set({
      volume: media.volume,
      muted: media.muted
    });
    sync();
    listen(media, "volumechange", sync, { signal });
  }
});
function canSetVolume() {
  const video = document.createElement("video");
  try {
    video.volume = 0.5;
    return video.volume === 0.5 ? "available" : "unsupported";
  } catch {
    return "unsupported";
  }
}

// node_modules/@videojs/core/dist/dev/dom/store/features/presets.js
var videoFeatures = [
  playbackFeature,
  playbackRateFeature,
  volumeFeature,
  timeFeature,
  sourceFeature,
  bufferFeature,
  fullscreenFeature,
  pipFeature,
  controlsFeature,
  textTrackFeature
];
var audioFeatures = [
  playbackFeature,
  playbackRateFeature,
  volumeFeature,
  timeFeature,
  sourceFeature,
  bufferFeature
];
var backgroundFeatures = [];

// node_modules/@videojs/core/dist/dev/_virtual/_rolldown/runtime.js
var __defProp = Object.defineProperty;
var __exportAll = (all, no_symbols) => {
  let target = {};
  for (var name in all) {
    __defProp(target, name, {
      get: all[name],
      enumerable: true
    });
  }
  if (!no_symbols) {
    __defProp(target, Symbol.toStringTag, { value: "Module" });
  }
  return target;
};

// node_modules/@videojs/core/dist/dev/dom/store/features/feature.parts.js
var feature_parts_exports = __exportAll({
  audioFeatures: () => audioFeatures,
  backgroundFeatures: () => backgroundFeatures,
  buffer: () => bufferFeature,
  controls: () => controlsFeature,
  fullscreen: () => fullscreenFeature,
  pip: () => pipFeature,
  playback: () => playbackFeature,
  playbackRate: () => playbackRateFeature,
  source: () => sourceFeature,
  textTrack: () => textTrackFeature,
  time: () => timeFeature,
  videoFeatures: () => videoFeatures,
  volume: () => volumeFeature
});

// node_modules/@videojs/core/dist/dev/dom/store/selectors.js
var selectBuffer = createSelector(bufferFeature);
var selectControls = createSelector(controlsFeature);
var selectFullscreen = createSelector(fullscreenFeature);
var selectPiP = createSelector(pipFeature);
var selectPlayback = createSelector(playbackFeature);
var selectPlaybackRate = createSelector(playbackRateFeature);
var selectSource = createSelector(sourceFeature);
var selectTextTrack = createSelector(textTrackFeature);
var selectTime = createSelector(timeFeature);
var selectVolume = createSelector(volumeFeature);

// node_modules/@videojs/core/dist/dev/dom/ui/button.js
function createButton(options) {
  const { onActivate, isDisabled } = options;
  return {
    role: "button",
    tabIndex: 0,
    onClick(event) {
      if (isDisabled()) {
        event.preventDefault();
        return;
      }
      onActivate();
    },
    onPointerDown(event) {
      if (isDisabled()) event.preventDefault();
    },
    onMouseDown(event) {
      if (isDisabled()) event.preventDefault();
    },
    onKeyDown(event) {
      if (event.target !== event.currentTarget) return;
      if (isDisabled()) {
        if (event.key !== "Tab") event.preventDefault();
        return;
      }
      if (event.key === "Enter") {
        event.preventDefault();
        onActivate();
      } else if (event.key === " ") event.preventDefault();
    },
    onKeyUp(event) {
      if (event.target !== event.currentTarget) return;
      if (isDisabled()) return;
      if (event.key === " ") onActivate();
    }
  };
}

// node_modules/@videojs/core/dist/dev/dom/ui/popover/popover.js
function createPopover(options) {
  const { transition, onOpenChange, closeOnEscape, closeOnOutsideClick } = options;
  const state = transition.state;
  let triggerEl = null;
  let popupEl = null;
  let hoverTimeout = null;
  const abort = new AbortController();
  let docAc = null;
  function clearHoverTimeout() {
    if (hoverTimeout !== null) {
      clearTimeout(hoverTimeout);
      hoverTimeout = null;
    }
  }
  function canHover() {
    var _a17, _b;
    return ((_b = (_a17 = globalThis.matchMedia) == null ? void 0 : _a17.call(globalThis, "(hover: hover)")) == null ? void 0 : _b.matches) ?? false;
  }
  function applyOpen(reason, event) {
    if (abort.signal.aborted) return;
    const { active, status } = state.current;
    if (active && status !== "ending") return;
    if (status === "ending") transition.cancel();
    transition.open().then(() => {
      var _a17;
      if (abort.signal.aborted || !state.current.active) return;
      (_a17 = options.onOpenChangeComplete) == null ? void 0 : _a17.call(options, true);
    });
    tryShowPopover(popupEl);
    onOpenChange(true, event ? {
      reason,
      event
    } : { reason });
  }
  function applyClose(reason, event) {
    const { active, status } = state.current;
    if (abort.signal.aborted || !active || status === "ending") return;
    transition.close(popupEl).then(() => {
      var _a17;
      if (abort.signal.aborted) return;
      tryHidePopover(popupEl);
      (_a17 = options.onOpenChangeComplete) == null ? void 0 : _a17.call(options, false);
    });
    onOpenChange(false, event ? {
      reason,
      event
    } : { reason });
  }
  function open(reason = "click") {
    applyOpen(reason);
  }
  function close(reason = "click") {
    applyClose(reason);
  }
  function setupDocumentListeners() {
    cleanupDocumentListeners();
    if (typeof document === "undefined") return;
    docAc = new AbortController();
    const signal = docAc.signal;
    listen(document, "keydown", handleDocumentKeydown, { signal });
    listen(document, "pointerdown", handleDocumentPointerdown, {
      capture: true,
      signal
    });
  }
  function cleanupDocumentListeners() {
    docAc == null ? void 0 : docAc.abort();
    docAc = null;
  }
  function handleDocumentKeydown(event) {
    if (event.key === "Escape" && closeOnEscape() && state.current.active) {
      event.preventDefault();
      applyClose("escape", event);
    }
  }
  function handleDocumentPointerdown(event) {
    if (!closeOnOutsideClick() || !state.current.active) return;
    const target = event.target;
    if (!target) return;
    if ((triggerEl == null ? void 0 : triggerEl.contains(target)) || (popupEl == null ? void 0 : popupEl.contains(target))) return;
    applyClose("outside-click", event);
  }
  const unsubscribe = state.subscribe(() => {
    if (state.current.active) setupDocumentListeners();
    else cleanupDocumentListeners();
  });
  abort.signal.addEventListener("abort", () => {
    unsubscribe();
    clearHoverTimeout();
    transition.destroy();
    cleanupDocumentListeners();
    triggerEl = null;
    popupEl = null;
  });
  const triggerProps = {
    onClick(event) {
      if (state.current.active && state.current.status !== "ending") applyClose("click", event);
      else applyOpen("click", event);
    },
    onPointerEnter(_event) {
      var _a17, _b;
      if (!((_a17 = options.openOnHover) == null ? void 0 : _a17.call(options))) return;
      if (!canHover()) return;
      clearHoverTimeout();
      if (state.current.active) return;
      const delay = ((_b = options.delay) == null ? void 0 : _b.call(options)) ?? 300;
      hoverTimeout = setTimeout(() => applyOpen("hover"), delay);
    },
    onPointerLeave(_event) {
      var _a17, _b;
      if (!((_a17 = options.openOnHover) == null ? void 0 : _a17.call(options))) return;
      if (!canHover()) return;
      clearHoverTimeout();
      if (!state.current.active) return;
      const closeDelay = ((_b = options.closeDelay) == null ? void 0 : _b.call(options)) ?? 0;
      hoverTimeout = setTimeout(() => applyClose("hover"), closeDelay);
    },
    onFocusIn(_event) {
      var _a17;
      if ((_a17 = options.openOnHover) == null ? void 0 : _a17.call(options)) applyOpen("focus");
    },
    onFocusOut(event) {
      var _a17;
      const relatedTarget = event.relatedTarget;
      if (relatedTarget && ((triggerEl == null ? void 0 : triggerEl.contains(relatedTarget)) || (popupEl == null ? void 0 : popupEl.contains(relatedTarget)))) return;
      if ((_a17 = options.openOnHover) == null ? void 0 : _a17.call(options)) applyClose("blur");
    }
  };
  const popupProps = {
    onPointerEnter(_event) {
      var _a17;
      if (!((_a17 = options.openOnHover) == null ? void 0 : _a17.call(options))) return;
      clearHoverTimeout();
    },
    onPointerLeave(_event) {
      var _a17, _b;
      if (!((_a17 = options.openOnHover) == null ? void 0 : _a17.call(options))) return;
      clearHoverTimeout();
      if (!state.current.active) return;
      const closeDelay = ((_b = options.closeDelay) == null ? void 0 : _b.call(options)) ?? 0;
      hoverTimeout = setTimeout(() => applyClose("hover"), closeDelay);
    },
    onFocusOut(event) {
      const relatedTarget = event.relatedTarget;
      if (relatedTarget && ((triggerEl == null ? void 0 : triggerEl.contains(relatedTarget)) || (popupEl == null ? void 0 : popupEl.contains(relatedTarget)))) return;
      applyClose("blur");
    }
  };
  function setTriggerElement(el) {
    triggerEl = el;
  }
  function setPopupElement(el) {
    if (!el && popupEl && state.current.active) tryHidePopover(popupEl);
    popupEl = el;
    if (el) {
      if (state.current.active) tryShowPopover(el);
    }
  }
  function destroy() {
    if (abort.signal.aborted) return;
    abort.abort();
  }
  return {
    input: state,
    triggerProps,
    popupProps,
    get triggerElement() {
      return triggerEl;
    },
    setTriggerElement,
    setPopupElement,
    open,
    close,
    destroy
  };
}
function tryShowPopover(el) {
  var _a17;
  try {
    (_a17 = el == null ? void 0 : el.showPopover) == null ? void 0 : _a17.call(el);
  } catch {
  }
}
function tryHidePopover(el) {
  var _a17;
  try {
    (_a17 = el == null ? void 0 : el.hidePopover) == null ? void 0 : _a17.call(el);
  } catch {
  }
}

// node_modules/@videojs/core/dist/dev/core/ui/popover/popover-css-vars.js
var PopoverCSSVars = {
  sideOffset: "--media-popover-side-offset",
  alignOffset: "--media-popover-align-offset",
  anchorWidth: "--media-popover-anchor-width",
  anchorHeight: "--media-popover-anchor-height",
  availableWidth: "--media-popover-available-width",
  availableHeight: "--media-popover-available-height"
};

// node_modules/@videojs/core/dist/dev/dom/ui/popover/popover-positioning.js
var OPPOSITE_SIDE = {
  top: "bottom",
  bottom: "top",
  left: "right",
  right: "left"
};
function getAnchorPositionStyle(anchorName, opts, triggerRect, popupRect, boundaryRect, offsets) {
  if (supportsAnchorPositioning()) return getAnchorPositionCSS(anchorName, opts);
  if (triggerRect && popupRect) return {
    ...getManualPositionStyle(triggerRect, popupRect, opts, offsets ?? {
      sideOffset: 0,
      alignOffset: 0
    }),
    ...boundaryRect ? getPopoverCSSVars(triggerRect, boundaryRect, opts.side) : {},
    position: "fixed",
    inset: "auto",
    margin: "0"
  };
  return {};
}
function getAnchorNameStyle(anchorName) {
  if (!supportsAnchorPositioning()) return {};
  return { anchorName: `--${anchorName}` };
}
var SIDE_OFFSET_VAR = `var(${PopoverCSSVars.sideOffset}, 0px)`;
var ALIGN_OFFSET_VAR = `var(${PopoverCSSVars.alignOffset}, 0px)`;
function getAnchorPositionCSS(anchorName, opts) {
  const { side, align } = opts;
  const style = {
    positionAnchor: `--${anchorName}`,
    position: "fixed",
    inset: "auto",
    margin: "0",
    justifySelf: "normal",
    alignSelf: "normal",
    marginInlineStart: "0",
    marginBlockStart: "0"
  };
  const insetProp = OPPOSITE_SIDE[side];
  if (side === "top" || side === "bottom") {
    style[insetProp] = `calc(anchor(${side}) + ${SIDE_OFFSET_VAR})`;
    if (align === "start") style.left = `calc(anchor(left) + ${ALIGN_OFFSET_VAR})`;
    else if (align === "end") style.right = `calc(anchor(right) + ${ALIGN_OFFSET_VAR})`;
    else {
      style.justifySelf = "anchor-center";
      style.marginInlineStart = ALIGN_OFFSET_VAR;
    }
  } else {
    style[insetProp] = `calc(anchor(${side}) + ${SIDE_OFFSET_VAR})`;
    if (align === "start") style.top = `calc(anchor(top) + ${ALIGN_OFFSET_VAR})`;
    else if (align === "end") style.bottom = `calc(anchor(bottom) + ${ALIGN_OFFSET_VAR})`;
    else {
      style.alignSelf = "anchor-center";
      style.marginBlockStart = ALIGN_OFFSET_VAR;
    }
  }
  return style;
}
function getPopoverCSSVars(triggerRect, boundaryRect, side) {
  const vars = {};
  vars[PopoverCSSVars.anchorWidth] = `${triggerRect.width}px`;
  vars[PopoverCSSVars.anchorHeight] = `${triggerRect.height}px`;
  if (side === "top" || side === "bottom") {
    vars[PopoverCSSVars.availableHeight] = side === "top" ? `${triggerRect.top - boundaryRect.top}px` : `${boundaryRect.bottom - triggerRect.bottom}px`;
    vars[PopoverCSSVars.availableWidth] = `${boundaryRect.width}px`;
  } else {
    vars[PopoverCSSVars.availableWidth] = side === "left" ? `${triggerRect.left - boundaryRect.left}px` : `${boundaryRect.right - triggerRect.right}px`;
    vars[PopoverCSSVars.availableHeight] = `${boundaryRect.height}px`;
  }
  return vars;
}
function getManualPositionStyle(triggerRect, popupRect, opts, offsets = {
  sideOffset: 0,
  alignOffset: 0
}) {
  const { side, align } = opts;
  const { sideOffset, alignOffset } = offsets;
  let top = 0;
  let left = 0;
  if (side === "top") top = triggerRect.top - popupRect.height - sideOffset;
  else if (side === "bottom") top = triggerRect.bottom + sideOffset;
  else if (side === "left") left = triggerRect.left - popupRect.width - sideOffset;
  else left = triggerRect.right + sideOffset;
  if (side === "top" || side === "bottom") if (align === "start") left = triggerRect.left + alignOffset;
  else if (align === "end") left = triggerRect.right - popupRect.width + alignOffset;
  else left = triggerRect.left + (triggerRect.width - popupRect.width) / 2 + alignOffset;
  else if (align === "start") top = triggerRect.top + alignOffset;
  else if (align === "end") top = triggerRect.bottom - popupRect.height + alignOffset;
  else top = triggerRect.top + (triggerRect.height - popupRect.height) / 2 + alignOffset;
  return {
    top: `${top}px`,
    left: `${left}px`
  };
}
function resolveOffsets(el) {
  const computed = getComputedStyle(el);
  return {
    sideOffset: Number.parseFloat(computed.getPropertyValue(PopoverCSSVars.sideOffset)) || 0,
    alignOffset: Number.parseFloat(computed.getPropertyValue(PopoverCSSVars.alignOffset)) || 0
  };
}

// node_modules/@videojs/utils/dist/number/number.js
function clamp(value, min, max) {
  return Math.max(min, Math.min(max, value));
}
function roundToStep(value, step, min) {
  const nearest = Math.round((value - min) / step) * step + min;
  const dot = `${step}`.indexOf(".");
  return dot === -1 ? nearest : Number(nearest.toFixed(`${step}`.length - dot - 1));
}

// node_modules/@videojs/core/dist/dev/dom/utils/pointer.js
function getPercentFromPointerEvent(event, rect, orientation, isRTL2) {
  let ratio;
  if (orientation === "vertical") ratio = 1 - (event.clientY - rect.top) / rect.height;
  else if (isRTL2) ratio = (rect.right - event.clientX) / rect.width;
  else ratio = (event.clientX - rect.left) / rect.width;
  if (!Number.isFinite(ratio)) return 0;
  return clamp(ratio * 100, 0, 100);
}

// node_modules/@videojs/core/dist/dev/dom/ui/slider.js
var DRAG_THRESHOLD = 2;
function createSlider(options) {
  const input = createState({
    pointerPercent: 0,
    dragPercent: 0,
    dragging: false,
    pointing: false,
    focused: false
  });
  const abort = new AbortController();
  const commitThrottleMs = options.commitThrottle ?? 0;
  let isDragging = false, moveCount = 0, cachedRTL = false, cachedRect = null, documentCleanup = null, capturedPointerId = null;
  const throttledCommit = commitThrottleMs > 0 ? throttle((percent) => {
    var _a17;
    return (_a17 = options.onValueCommit) == null ? void 0 : _a17.call(options, percent);
  }, commitThrottleMs) : null;
  function releaseCapture() {
    if (isNull(capturedPointerId)) return;
    const id = capturedPointerId;
    capturedPointerId = null;
    try {
      options.getElement().releasePointerCapture(id);
    } catch {
    }
  }
  function endDrag() {
    var _a17;
    if (!isDragging) input.patch({
      pointing: false,
      pointerPercent: 0
    });
    else {
      isDragging = false;
      input.patch({
        dragging: false,
        pointing: false,
        pointerPercent: 0
      });
      (_a17 = options.onDragEnd) == null ? void 0 : _a17.call(options);
    }
    cleanup();
  }
  function cleanup() {
    throttledCommit == null ? void 0 : throttledCommit.cancel();
    releaseCapture();
    documentCleanup == null ? void 0 : documentCleanup();
    documentCleanup = null;
    cachedRect = null;
  }
  function onDocumentPointerMove(event) {
    var _a17, _b, _c;
    if (event.pointerType !== "touch" && event.buttons === 0) {
      endDrag();
      return;
    }
    moveCount++;
    const percent = getPercentFromPointerEvent(event, cachedRect, options.getOrientation(), cachedRTL);
    if (!isDragging && moveCount >= DRAG_THRESHOLD) {
      isDragging = true;
      input.patch({
        dragging: true,
        dragPercent: percent,
        pointerPercent: percent
      });
      (_a17 = options.onDragStart) == null ? void 0 : _a17.call(options);
      (_b = options.onValueChange) == null ? void 0 : _b.call(options, percent);
      throttledCommit == null ? void 0 : throttledCommit(percent);
    } else if (isDragging) {
      input.patch({
        dragPercent: percent,
        pointerPercent: percent
      });
      (_c = options.onValueChange) == null ? void 0 : _c.call(options, percent);
      throttledCommit == null ? void 0 : throttledCommit(percent);
    } else input.patch({ pointerPercent: percent });
  }
  function onDocumentPointerUp(event) {
    var _a17;
    const percent = getPercentFromPointerEvent(event, cachedRect, options.getOrientation(), cachedRTL);
    throttledCommit == null ? void 0 : throttledCommit.cancel();
    (_a17 = options.onValueCommit) == null ? void 0 : _a17.call(options, percent);
    endDrag();
  }
  function addDocumentListeners() {
    const abort2 = new AbortController();
    const signal = abort2.signal;
    listen(document, "pointermove", onDocumentPointerMove, {
      passive: true,
      signal
    });
    listen(document, "pointerup", onDocumentPointerUp, { signal });
    listen(document, "pointercancel", endDrag, { signal });
    listen(document, "touchmove", (e) => e.preventDefault(), {
      passive: false,
      signal
    });
    documentCleanup = () => abort2.abort();
  }
  const rootProps = {
    onPointerDown(event) {
      var _a17, _b, _c;
      if (options.isDisabled()) return;
      const el = options.getElement();
      cachedRect = el.getBoundingClientRect();
      cachedRTL = options.isRTL();
      moveCount = 0;
      releaseCapture();
      capturedPointerId = event.pointerId;
      el.setPointerCapture(event.pointerId);
      const percent = getPercentFromPointerEvent(event, cachedRect, options.getOrientation(), cachedRTL);
      input.patch({
        pointing: true,
        pointerPercent: percent,
        dragPercent: percent
      });
      (_a17 = options.onValueChange) == null ? void 0 : _a17.call(options, percent);
      (_c = (_b = options.getThumbElement) == null ? void 0 : _b.call(options)) == null ? void 0 : _c.focus();
      documentCleanup == null ? void 0 : documentCleanup();
      addDocumentListeners();
    },
    onPointerMove(event) {
      if (options.isDisabled() || isDragging) return;
      const percent = getPercentFromPointerEvent(event, options.getElement().getBoundingClientRect(), options.getOrientation(), options.isRTL());
      input.patch({
        pointing: true,
        pointerPercent: percent
      });
    },
    onPointerLeave() {
      if (isDragging) return;
      input.patch({
        pointing: false,
        pointerPercent: 0
      });
    }
  };
  const thumbProps = {
    onKeyDown(event) {
      var _a17, _b;
      if (options.isDisabled()) {
        if (event.key !== "Tab") event.preventDefault();
        return;
      }
      const stepPercent = options.getStepPercent();
      const largeStepPercent = options.getLargeStepPercent();
      const rounded = roundToStep(options.getPercent(), stepPercent, 0);
      const horizontalSign = options.isRTL() ? -1 : 1;
      const step = event.shiftKey ? largeStepPercent : stepPercent;
      let newPercent = null;
      switch (event.key) {
        case "ArrowRight":
          newPercent = rounded + step * horizontalSign;
          break;
        case "ArrowLeft":
          newPercent = rounded - step * horizontalSign;
          break;
        case "ArrowUp":
          newPercent = rounded + step;
          break;
        case "ArrowDown":
          newPercent = rounded - step;
          break;
        case "PageUp":
          newPercent = rounded + largeStepPercent;
          break;
        case "PageDown":
          newPercent = rounded - largeStepPercent;
          break;
        case "Home":
          newPercent = 0;
          break;
        case "End":
          newPercent = 100;
          break;
        default:
          if (!event.metaKey && !event.ctrlKey && !event.altKey && event.key >= "0" && event.key <= "9") newPercent = Number(event.key) * 10;
          break;
      }
      if (newPercent !== null) {
        event.preventDefault();
        newPercent = clamp(newPercent, 0, 100);
        input.patch({
          pointerPercent: newPercent,
          dragPercent: newPercent
        });
        (_a17 = options.onValueChange) == null ? void 0 : _a17.call(options, newPercent);
        (_b = options.onValueCommit) == null ? void 0 : _b.call(options, newPercent);
      }
    },
    onFocus() {
      input.patch({ focused: true });
    },
    onBlur() {
      input.patch({ focused: false });
    }
  };
  listen(abort.signal, "abort", cleanup, { once: true });
  return {
    input,
    rootProps,
    thumbProps,
    destroy() {
      abort.abort();
    }
  };
}

// node_modules/@videojs/core/dist/dev/core/ui/slider/slider-css-vars.js
var SliderCSSVars = {
  fill: "--media-slider-fill",
  pointer: "--media-slider-pointer",
  buffer: "--media-slider-buffer"
};

// node_modules/@videojs/core/dist/dev/dom/ui/slider-css-vars.js
function getSliderCSSVars(state) {
  return {
    [SliderCSSVars.fill]: `${state.fillPercent.toFixed(3)}%`,
    [SliderCSSVars.pointer]: `${state.pointerPercent.toFixed(3)}%`
  };
}
function getTimeSliderCSSVars(state) {
  return {
    ...getSliderCSSVars(state),
    [SliderCSSVars.buffer]: `${state.bufferPercent.toFixed(3)}%`
  };
}

// node_modules/@videojs/core/dist/dev/core/ui/thumbnail/thumbnail-core.js
var ThumbnailCore = class {
  findActiveThumbnail(thumbnails, time) {
    if (thumbnails.length === 0) return void 0;
    let low = 0;
    let high = thumbnails.length - 1;
    let result;
    while (low <= high) {
      const mid = low + high >>> 1;
      const image = thumbnails[mid];
      if (time >= image.startTime) {
        result = image;
        low = mid + 1;
      } else high = mid - 1;
    }
    return result;
  }
  /**
  * Parse CSS constraint strings into numeric `ThumbnailConstraints`.
  *
  * Accepts any object with string `minWidth`/`maxWidth`/`minHeight`/`maxHeight`
  * properties — `CSSStyleDeclaration` satisfies this structurally.
  */
  parseConstraints(raw) {
    const minW = parseFloat(raw.minWidth);
    const maxW = parseFloat(raw.maxWidth);
    const minH = parseFloat(raw.minHeight);
    const maxH = parseFloat(raw.maxHeight);
    return {
      minWidth: Number.isFinite(minW) ? minW : 0,
      maxWidth: Number.isFinite(maxW) ? maxW : Infinity,
      minHeight: Number.isFinite(minH) ? minH : 0,
      maxHeight: Number.isFinite(maxH) ? maxH : Infinity
    };
  }
  /**
  * Calculate a uniform scale factor that fits `tileWidth × tileHeight` within the
  * given CSS min/max constraints while preserving aspect ratio.
  *
  * - Scales down when the tile exceeds max constraints.
  * - Scales up when the tile is smaller than min constraints.
  * - Returns `1` when no scaling is needed.
  */
  calculateScale(tileWidth, tileHeight, constraints) {
    const { minWidth, maxWidth, minHeight, maxHeight } = constraints;
    const maxRatio = Math.min(maxWidth / tileWidth, maxHeight / tileHeight);
    const minRatio = Math.max(minWidth / tileWidth, minHeight / tileHeight);
    if (Number.isFinite(maxRatio) && maxRatio < 1) return maxRatio;
    if (Number.isFinite(minRatio) && minRatio > 1) return minRatio;
    return 1;
  }
  /**
  * Compute container and image dimensions for the current thumbnail, scaled to
  * fit within the element's CSS min/max constraints.
  *
  * The container clips the sprite sheet via `overflow: hidden`, and the image is
  * positioned with `transform: translate()` to show the correct tile.
  */
  resize(thumbnail, imgNaturalWidth, imgNaturalHeight, constraints) {
    var _a17, _b;
    const tileWidth = thumbnail.width ?? imgNaturalWidth;
    const tileHeight = thumbnail.height ?? imgNaturalHeight;
    if (!tileWidth || !tileHeight) return void 0;
    const scale = this.calculateScale(tileWidth, tileHeight, constraints);
    return {
      scale,
      containerWidth: tileWidth * scale,
      containerHeight: tileHeight * scale,
      imageWidth: imgNaturalWidth * scale,
      imageHeight: imgNaturalHeight * scale,
      offsetX: (((_a17 = thumbnail.coords) == null ? void 0 : _a17.x) ?? 0) * scale,
      offsetY: (((_b = thumbnail.coords) == null ? void 0 : _b.y) ?? 0) * scale
    };
  }
  getState(loading, error, thumbnail) {
    return {
      loading,
      error,
      hidden: !loading && !thumbnail
    };
  }
  getAttrs(_state) {
    return {
      role: "img",
      "aria-hidden": "true"
    };
  }
};

// node_modules/@videojs/core/dist/dev/dom/ui/thumbnail.js
function createThumbnail(options) {
  const { getContainer, getImg, onStateChange } = options;
  const core = new ThumbnailCore();
  const abort = new AbortController();
  const signal = abort.signal;
  let loading = false;
  let error = false;
  let naturalWidth = 0;
  let naturalHeight = 0;
  let lastSrc = "";
  let imgBound = false;
  let resizeObserver = null;
  function onImgLoad() {
    const img = getImg();
    if (img) {
      naturalWidth = img.naturalWidth;
      naturalHeight = img.naturalHeight;
    }
    loading = false;
    error = false;
    onStateChange();
  }
  function onImgError() {
    loading = false;
    error = true;
    onStateChange();
  }
  function bindImg(img) {
    listen(img, "load", onImgLoad, { signal });
    listen(img, "error", onImgError, { signal });
  }
  function ensureBindings() {
    if (!imgBound) {
      const img = getImg();
      if (img) {
        bindImg(img);
        imgBound = true;
      }
    }
    if (!resizeObserver) {
      const container = getContainer();
      if (container) {
        resizeObserver = new ResizeObserver(onStateChange);
        resizeObserver.observe(container);
      }
    }
  }
  function updateSrc(url) {
    ensureBindings();
    const src = url ?? "";
    if (src === lastSrc) return;
    lastSrc = src;
    if (src) {
      loading = true;
      error = false;
    } else {
      loading = false;
      error = false;
      naturalWidth = 0;
      naturalHeight = 0;
    }
  }
  function connect() {
    ensureBindings();
    const img = getImg();
    if ((img == null ? void 0 : img.complete) && img.naturalWidth > 0 && lastSrc) {
      naturalWidth = img.naturalWidth;
      naturalHeight = img.naturalHeight;
      loading = false;
      error = false;
      onStateChange();
    }
  }
  function destroy() {
    abort.abort();
    resizeObserver == null ? void 0 : resizeObserver.disconnect();
    resizeObserver = null;
  }
  return {
    get loading() {
      return loading;
    },
    get error() {
      return error;
    },
    get naturalWidth() {
      return naturalWidth;
    },
    get naturalHeight() {
      return naturalHeight;
    },
    readConstraints() {
      const el = getContainer();
      if (!el) return {
        minWidth: 0,
        maxWidth: Infinity,
        minHeight: 0,
        maxHeight: Infinity
      };
      return core.parseConstraints(getComputedStyle(el));
    },
    updateSrc,
    connect,
    destroy
  };
}

// node_modules/@videojs/core/dist/dev/dom/ui/transition.js
function createTransition() {
  const state = createState({
    active: false,
    status: "idle"
  });
  let destroyed = false;
  let rafId1 = 0;
  let rafId2 = 0;
  function open() {
    cancelAnimationFrame(rafId1);
    cancelAnimationFrame(rafId2);
    rafId1 = 0;
    rafId2 = 0;
    state.patch({
      active: true,
      status: "starting"
    });
    return new Promise((resolve) => {
      rafId1 = requestAnimationFrame(() => {
        rafId1 = 0;
        if (destroyed || !state.current.active) return resolve();
        state.patch({ status: "idle" });
        resolve();
      });
    });
  }
  function close(el) {
    cancelAnimationFrame(rafId1);
    cancelAnimationFrame(rafId2);
    rafId1 = 0;
    rafId2 = 0;
    state.patch({ status: "ending" });
    return new Promise((resolve) => {
      rafId1 = requestAnimationFrame(() => {
        rafId1 = 0;
        rafId2 = requestAnimationFrame(() => {
          rafId2 = 0;
          if (destroyed) return resolve();
          waitForAnimations(el).finally(() => {
            if (destroyed || state.current.status !== "ending") return resolve();
            state.patch({
              active: false,
              status: "idle"
            });
            resolve();
          });
        });
      });
    });
  }
  function cancel() {
    cancelAnimationFrame(rafId1);
    cancelAnimationFrame(rafId2);
    rafId1 = 0;
    rafId2 = 0;
    if (state.current.status !== "idle") state.patch({ status: "idle" });
  }
  return {
    state,
    open,
    close,
    cancel,
    destroy() {
      if (destroyed) return;
      destroyed = true;
      cancel();
    }
  };
}
function waitForAnimations(el) {
  var _a17;
  if (!el) return Promise.resolve();
  const animations = ((_a17 = el.getAnimations) == null ? void 0 : _a17.call(el)) ?? [];
  if (animations.length === 0) return Promise.resolve();
  return Promise.all(animations.map((a) => a.finished)).then(noop, noop);
}

// node_modules/@videojs/core/dist/dev/dom/utils/element-props.js
function applyElementProps(element, props, options) {
  const signal = options == null ? void 0 : options.signal;
  for (const [key, value] of Object.entries(props)) if (isFunction(value) && key.startsWith("on")) listen(element, key.slice(2).toLowerCase(), value, signal ? { signal } : void 0);
  else if (isUndefined(value) || value === false) element.removeAttribute(key);
  else if (value === true) element.setAttribute(key, "");
  else element.setAttribute(key, String(value));
}

// node_modules/@videojs/core/dist/dev/dom/utils/log.js
var warned = /* @__PURE__ */ new Set();
function logMissingFeature(displayName, featureName) {
  const key = `${displayName}:${featureName}`;
  if (warned.has(key)) return;
  warned.add(key);
  console.warn(`${displayName} requires ${featureName} feature`);
}

// node_modules/@videojs/core/dist/dev/dom/utils/state-data-attrs.js
function getStateDataAttrs(state, map) {
  const attrs = {};
  for (const key in state) {
    if (map && !(key in map)) continue;
    const name = (map == null ? void 0 : map[key]) ?? toDataAttrName(key), value = state[key];
    if (value === true) attrs[name] = "";
    else if (value) attrs[name] = String(value);
  }
  return attrs;
}
function applyStateDataAttrs(element, state, map) {
  for (const key in state) {
    if (map && !(key in map)) continue;
    const name = (map == null ? void 0 : map[key]) ?? toDataAttrName(key), value = state[key];
    if (value === true) element.setAttribute(name, "");
    else if (value) element.setAttribute(name, String(value));
    else element.removeAttribute(name);
  }
}
function toDataAttrName(key) {
  return `data-${key.toLowerCase()}`;
}

// node_modules/@videojs/react/dist/dev/utils/use-render.js
var import_react5 = __toESM(require_react(), 1);
function resolveClassName(className, state) {
  return isFunction(className) ? className(state) : className;
}
function resolveStyle(style, state) {
  return isFunction(style) ? style(state) : style;
}
function getElementRef(element) {
  var _a17;
  const elementAny = element;
  return elementAny.ref ?? ((_a17 = elementAny.props) == null ? void 0 : _a17.ref);
}
function renderElement(element, componentProps, params) {
  const { className: classNameProp, style: styleProp, render } = componentProps;
  const { state, ref, props, stateAttrMap } = params;
  const className = resolveClassName(classNameProp, state);
  const style = resolveStyle(styleProp, state);
  const mergedProps = mergeProps(getStateDataAttrs(state, stateAttrMap), ...Array.isArray(props) ? props : props ? [props] : []);
  if (className !== void 0) mergedProps.className = mergedProps.className ? `${mergedProps.className} ${className}` : className;
  if (style !== void 0) mergedProps.style = mergedProps.style ? {
    ...mergedProps.style,
    ...style
  } : style;
  if (isFunction(render)) {
    const mergedRef = composeRefs(ref, mergedProps.ref);
    return render({
      ...mergedProps,
      ref: mergedRef
    }, state);
  }
  if ((0, import_react5.isValidElement)(render)) {
    const elementRef = getElementRef(render);
    const mergedRef = composeRefs(ref, mergedProps.ref, elementRef);
    const elementProps = mergeProps(mergedProps, render.props);
    elementProps.ref = mergedRef;
    return (0, import_react5.cloneElement)(render, elementProps);
  }
  mergedProps.ref = composeRefs(ref, mergedProps.ref);
  return (0, import_react5.createElement)(element, mergedProps);
}

// node_modules/@videojs/react/dist/dev/ui/buffering-indicator/buffering-indicator.js
var import_react6 = __toESM(require_react(), 1);

// node_modules/@videojs/core/dist/dev/core/ui/buffering-indicator/buffering-indicator-core.js
var _a3, _props, _timer;
var BufferingIndicatorCore = (_a3 = class {
  constructor() {
    __publicField(this, "state", createState({ visible: false }));
    __privateAdd(this, _props, { ..._a3.defaultProps });
    __privateAdd(this, _timer, null);
  }
  setProps(props) {
    __privateSet(this, _props, defaults(props, _a3.defaultProps));
  }
  update(media) {
    const buffering = media.waiting && !media.paused;
    if (buffering && !this.state.current.visible && !__privateGet(this, _timer)) __privateSet(this, _timer, setTimeout(() => {
      __privateSet(this, _timer, null);
      this.state.patch({ visible: true });
    }, __privateGet(this, _props).delay));
    else if (!buffering) {
      if (__privateGet(this, _timer) !== null) {
        clearTimeout(__privateGet(this, _timer));
        __privateSet(this, _timer, null);
      }
      this.state.patch({ visible: false });
    }
  }
}, _props = new WeakMap(), _timer = new WeakMap(), __publicField(_a3, "defaultProps", { delay: 500 }), _a3);

// node_modules/@videojs/core/dist/dev/core/ui/buffering-indicator/buffering-indicator-data-attrs.js
var BufferingIndicatorDataAttrs = { visible: "data-visible" };

// node_modules/@videojs/core/dist/dev/core/ui/controls/controls-core.js
var _media, _a4;
var ControlsCore = (_a4 = class {
  constructor() {
    __privateAdd(this, _media, null);
  }
  setMedia(media) {
    __privateSet(this, _media, media);
  }
  getState() {
    const media = __privateGet(this, _media);
    return {
      visible: media.controlsVisible,
      userActive: media.userActive
    };
  }
}, _media = new WeakMap(), _a4);

// node_modules/@videojs/core/dist/dev/core/ui/controls/controls-data-attrs.js
var ControlsDataAttrs = {
  visible: "data-visible",
  userActive: "data-user-active"
};

// node_modules/@videojs/core/dist/dev/core/ui/fullscreen-button/fullscreen-button-core.js
var _a5, _props2, _media2;
var FullscreenButtonCore = (_a5 = class {
  constructor(props) {
    __privateAdd(this, _props2, { ..._a5.defaultProps });
    __privateAdd(this, _media2, null);
    if (props) this.setProps(props);
  }
  setProps(props) {
    __privateSet(this, _props2, defaults(props, _a5.defaultProps));
  }
  getLabel(state) {
    const { label } = __privateGet(this, _props2);
    if (isFunction(label)) {
      const customLabel = label(state);
      if (customLabel) return customLabel;
    } else if (label) return label;
    return state.fullscreen ? "Exit fullscreen" : "Enter fullscreen";
  }
  getAttrs(state) {
    return {
      "aria-label": this.getLabel(state),
      "aria-disabled": __privateGet(this, _props2).disabled ? "true" : void 0
    };
  }
  setMedia(media) {
    __privateSet(this, _media2, media);
  }
  getState() {
    const media = __privateGet(this, _media2);
    return {
      fullscreen: media.fullscreen,
      availability: media.fullscreenAvailability
    };
  }
  async toggle(media) {
    if (__privateGet(this, _props2).disabled) return;
    if (media.fullscreenAvailability !== "available") return;
    try {
      if (media.fullscreen) await media.exitFullscreen();
      else await media.requestFullscreen();
    } catch {
    }
  }
}, _props2 = new WeakMap(), _media2 = new WeakMap(), __publicField(_a5, "defaultProps", {
  label: "",
  disabled: false
}), _a5);

// node_modules/@videojs/core/dist/dev/core/ui/fullscreen-button/fullscreen-button-data-attrs.js
var FullscreenButtonDataAttrs = {
  fullscreen: "data-fullscreen",
  availability: "data-availability"
};

// node_modules/@videojs/core/dist/dev/core/ui/mute-button/mute-button-core.js
var _a6, _props3, _media3;
var MuteButtonCore = (_a6 = class {
  constructor(props) {
    __privateAdd(this, _props3, { ..._a6.defaultProps });
    __privateAdd(this, _media3, null);
    if (props) this.setProps(props);
  }
  setProps(props) {
    __privateSet(this, _props3, defaults(props, _a6.defaultProps));
  }
  getLabel(state) {
    const { label } = __privateGet(this, _props3);
    if (isFunction(label)) {
      const customLabel = label(state);
      if (customLabel) return customLabel;
    } else if (label) return label;
    return state.muted ? "Unmute" : "Mute";
  }
  getAttrs(state) {
    return {
      "aria-label": this.getLabel(state),
      "aria-disabled": __privateGet(this, _props3).disabled ? "true" : void 0
    };
  }
  setMedia(media) {
    __privateSet(this, _media3, media);
  }
  getState() {
    const media = __privateGet(this, _media3);
    return {
      muted: media.muted,
      volumeLevel: getVolumeLevel(media)
    };
  }
  toggle(media) {
    if (__privateGet(this, _props3).disabled) return;
    media.toggleMute();
  }
}, _props3 = new WeakMap(), _media3 = new WeakMap(), __publicField(_a6, "defaultProps", {
  label: "",
  disabled: false
}), _a6);
function getVolumeLevel(media) {
  if (media.muted || media.volume === 0) return "off";
  if (media.volume < 0.5) return "low";
  if (media.volume < 0.75) return "medium";
  return "high";
}

// node_modules/@videojs/core/dist/dev/core/ui/mute-button/mute-button-data-attrs.js
var MuteButtonDataAttrs = {
  muted: "data-muted",
  volumeLevel: "data-volume-level"
};

// node_modules/@videojs/core/dist/dev/core/ui/pip-button/pip-button-core.js
var _a7, _props4, _media4;
var PiPButtonCore = (_a7 = class {
  constructor(props) {
    __privateAdd(this, _props4, { ..._a7.defaultProps });
    __privateAdd(this, _media4, null);
    if (props) this.setProps(props);
  }
  setProps(props) {
    __privateSet(this, _props4, defaults(props, _a7.defaultProps));
  }
  getLabel(state) {
    const { label } = __privateGet(this, _props4);
    if (isFunction(label)) {
      const customLabel = label(state);
      if (customLabel) return customLabel;
    } else if (label) return label;
    return state.pip ? "Exit picture-in-picture" : "Enter picture-in-picture";
  }
  getAttrs(state) {
    return {
      "aria-label": this.getLabel(state),
      "aria-disabled": __privateGet(this, _props4).disabled ? "true" : void 0
    };
  }
  setMedia(media) {
    __privateSet(this, _media4, media);
  }
  getState() {
    const media = __privateGet(this, _media4);
    return {
      pip: media.pip,
      availability: media.pipAvailability
    };
  }
  async toggle(media) {
    if (__privateGet(this, _props4).disabled) return;
    if (media.pipAvailability !== "available") return;
    try {
      if (media.pip) await media.exitPiP();
      else await media.requestPiP();
    } catch {
    }
  }
}, _props4 = new WeakMap(), _media4 = new WeakMap(), __publicField(_a7, "defaultProps", {
  label: "",
  disabled: false
}), _a7);

// node_modules/@videojs/core/dist/dev/core/ui/pip-button/pip-button-data-attrs.js
var PiPButtonDataAttrs = {
  pip: "data-pip",
  availability: "data-availability"
};

// node_modules/@videojs/core/dist/dev/core/ui/play-button/play-button-core.js
var _a8, _props5, _media5;
var PlayButtonCore = (_a8 = class {
  constructor(props) {
    __privateAdd(this, _props5, { ..._a8.defaultProps });
    __privateAdd(this, _media5, null);
    if (props) this.setProps(props);
  }
  setProps(props) {
    __privateSet(this, _props5, defaults(props, _a8.defaultProps));
  }
  getLabel(state) {
    const { label } = __privateGet(this, _props5);
    if (isFunction(label)) {
      const customLabel = label(state);
      if (customLabel) return customLabel;
    } else if (label) return label;
    if (state.ended) return "Replay";
    return state.paused ? "Play" : "Pause";
  }
  getAttrs(state) {
    return {
      "aria-label": this.getLabel(state),
      "aria-disabled": __privateGet(this, _props5).disabled ? "true" : void 0
    };
  }
  setMedia(media) {
    __privateSet(this, _media5, media);
  }
  getState() {
    const media = __privateGet(this, _media5);
    return {
      paused: media.paused,
      ended: media.ended,
      started: media.started
    };
  }
  async toggle(media) {
    if (__privateGet(this, _props5).disabled) return;
    if (media.paused || media.ended) return media.play();
    media.pause();
  }
}, _props5 = new WeakMap(), _media5 = new WeakMap(), __publicField(_a8, "defaultProps", {
  label: "",
  disabled: false
}), _a8);

// node_modules/@videojs/core/dist/dev/core/ui/play-button/play-button-data-attrs.js
var PlayButtonDataAttrs = {
  paused: "data-paused",
  ended: "data-ended",
  started: "data-started"
};

// node_modules/@videojs/core/dist/dev/core/ui/playback-rate-button/playback-rate-button-core.js
var _a9, _props6, _media6;
var PlaybackRateButtonCore = (_a9 = class {
  constructor(props) {
    __privateAdd(this, _props6, { ..._a9.defaultProps });
    __privateAdd(this, _media6, null);
    if (props) this.setProps(props);
  }
  setProps(props) {
    __privateSet(this, _props6, defaults(props, _a9.defaultProps));
  }
  getLabel(state) {
    const { label } = __privateGet(this, _props6);
    if (isFunction(label)) {
      const customLabel = label(state);
      if (customLabel) return customLabel;
    } else if (label) return label;
    return `Playback rate ${state.rate}`;
  }
  getAttrs(state) {
    return {
      "aria-label": this.getLabel(state),
      "aria-disabled": __privateGet(this, _props6).disabled ? "true" : void 0
    };
  }
  setMedia(media) {
    __privateSet(this, _media6, media);
  }
  getState() {
    return { rate: __privateGet(this, _media6).playbackRate };
  }
  cycle(media) {
    if (__privateGet(this, _props6).disabled) return;
    const { playbackRates, playbackRate } = media;
    if (playbackRates.length === 0) return;
    const idx = playbackRates.indexOf(playbackRate);
    const next = idx === -1 ? playbackRates.find((r) => r > playbackRate) ?? playbackRates[0] : playbackRates[(idx + 1) % playbackRates.length];
    media.setPlaybackRate(next);
  }
}, _props6 = new WeakMap(), _media6 = new WeakMap(), __publicField(_a9, "defaultProps", {
  label: "",
  disabled: false
}), _a9);

// node_modules/@videojs/core/dist/dev/core/ui/playback-rate-button/playback-rate-button-data-attrs.js
var PlaybackRateButtonDataAttrs = { rate: "data-rate" };

// node_modules/@videojs/core/dist/dev/core/ui/transition.js
function getTransitionFlags(status) {
  return {
    transitionStarting: status === "starting",
    transitionEnding: status === "ending"
  };
}

// node_modules/@videojs/core/dist/dev/core/ui/popover/popover-core.js
var _a10, _props7, _input;
var PopoverCore = (_a10 = class {
  constructor(props) {
    __privateAdd(this, _props7, { ..._a10.defaultProps });
    __privateAdd(this, _input, null);
    if (props) this.setProps(props);
  }
  setProps(props) {
    __privateSet(this, _props7, defaults(props, _a10.defaultProps));
  }
  setInput(input) {
    __privateSet(this, _input, input);
  }
  getState() {
    const input = __privateGet(this, _input);
    return {
      open: input.active,
      status: input.status,
      side: __privateGet(this, _props7).side,
      align: __privateGet(this, _props7).align,
      modal: __privateGet(this, _props7).modal,
      ...getTransitionFlags(input.status)
    };
  }
  getTriggerAttrs(state, popupId) {
    return {
      "aria-expanded": state.open ? "true" : "false",
      "aria-haspopup": "dialog",
      "aria-controls": popupId
    };
  }
  getPopupAttrs(state) {
    return {
      popover: "manual",
      role: "dialog",
      "aria-modal": state.modal === true ? "true" : void 0
    };
  }
}, _props7 = new WeakMap(), _input = new WeakMap(), __publicField(_a10, "defaultProps", {
  side: "top",
  align: "center",
  modal: false,
  closeOnEscape: true,
  closeOnOutsideClick: true,
  open: false,
  defaultOpen: false,
  openOnHover: false,
  delay: 300,
  closeDelay: 0
}), _a10);

// node_modules/@videojs/core/dist/dev/core/ui/popover/popover-data-attrs.js
var PopoverDataAttrs = {
  open: "data-open",
  side: "data-side",
  align: "data-align",
  transitionStarting: "data-starting-style",
  transitionEnding: "data-ending-style"
};

// node_modules/@videojs/core/dist/dev/core/ui/poster/poster-core.js
var _media7, _a11;
var PosterCore = (_a11 = class {
  constructor() {
    __privateAdd(this, _media7, null);
  }
  setMedia(media) {
    __privateSet(this, _media7, media);
  }
  getState() {
    return { visible: !__privateGet(this, _media7).started };
  }
}, _media7 = new WeakMap(), _a11);

// node_modules/@videojs/core/dist/dev/core/ui/poster/poster-data-attrs.js
var PosterDataAttrs = { visible: "data-visible" };

// node_modules/@videojs/core/dist/dev/core/ui/seek-button/seek-button-core.js
var _a12, _props8, _media8;
var SeekButtonCore = (_a12 = class {
  constructor(props) {
    __privateAdd(this, _props8, { ..._a12.defaultProps });
    __privateAdd(this, _media8, null);
    if (props) this.setProps(props);
  }
  setProps(props) {
    __privateSet(this, _props8, defaults(props, _a12.defaultProps));
  }
  getLabel(state) {
    const { label } = __privateGet(this, _props8);
    if (isFunction(label)) {
      const customLabel = label(state);
      if (customLabel) return customLabel;
    } else if (label) return label;
    const abs = Math.abs(__privateGet(this, _props8).seconds);
    return state.direction === "backward" ? `Seek backward ${abs} seconds` : `Seek forward ${abs} seconds`;
  }
  getAttrs(state) {
    return {
      "aria-label": this.getLabel(state),
      "aria-disabled": __privateGet(this, _props8).disabled ? "true" : void 0
    };
  }
  setMedia(media) {
    __privateSet(this, _media8, media);
  }
  getState() {
    return {
      seeking: __privateGet(this, _media8).seeking,
      direction: __privateGet(this, _props8).seconds < 0 ? "backward" : "forward"
    };
  }
  async seek(media) {
    if (__privateGet(this, _props8).disabled) return;
    await media.seek(media.currentTime + __privateGet(this, _props8).seconds);
  }
}, _props8 = new WeakMap(), _media8 = new WeakMap(), __publicField(_a12, "defaultProps", {
  seconds: 30,
  label: "",
  disabled: false
}), _a12);

// node_modules/@videojs/core/dist/dev/core/ui/seek-button/seek-button-data-attrs.js
var SeekButtonDataAttrs = {
  seeking: "data-seeking",
  direction: "data-direction"
};

// node_modules/@videojs/core/dist/dev/core/ui/slider/slider-core.js
var _a13, _props9, _input2;
var SliderCore = (_a13 = class {
  constructor(props) {
    __privateAdd(this, _props9, { ..._a13.defaultProps });
    __privateAdd(this, _input2, { ..._a13.defaultInput });
    if (props) this.setProps(props);
  }
  get props() {
    return __privateGet(this, _props9);
  }
  get input() {
    return __privateGet(this, _input2);
  }
  setProps(props) {
    __privateSet(this, _props9, defaults(props, _a13.defaultProps));
  }
  setInput(input) {
    __privateSet(this, _input2, input);
  }
  getSliderState(value) {
    const { orientation, disabled, thumbAlignment } = __privateGet(this, _props9);
    const { pointerPercent, dragging, pointing, focused } = __privateGet(this, _input2);
    return {
      value,
      fillPercent: this.percentFromValue(value),
      pointerPercent,
      dragging,
      pointing,
      interactive: dragging || pointing || focused,
      orientation,
      disabled,
      thumbAlignment
    };
  }
  getLabel(state) {
    const { label } = __privateGet(this, _props9);
    if (isFunction(label)) {
      const customLabel = label(state);
      if (customLabel) return customLabel;
    } else if (label) return label;
    return "";
  }
  getAttrs(state) {
    return {
      role: "slider",
      tabIndex: state.disabled ? -1 : 0,
      autoComplete: "off",
      "aria-label": this.getLabel(state),
      "aria-valuemin": __privateGet(this, _props9).min,
      "aria-valuemax": __privateGet(this, _props9).max,
      "aria-valuenow": state.value,
      "aria-orientation": state.orientation,
      "aria-disabled": state.disabled ? "true" : void 0
    };
  }
  valueFromPercent(percent) {
    const { min, max, step } = __privateGet(this, _props9);
    return roundToStep(clamp(min + percent / 100 * (max - min), min, max), step, min);
  }
  percentFromValue(value) {
    const { min, max } = __privateGet(this, _props9);
    if (max === min) return 0;
    return (value - min) / (max - min) * 100;
  }
  /** Step as a percentage of the slider range. */
  getStepPercent() {
    const { step, min, max } = __privateGet(this, _props9);
    const range = max - min;
    return range > 0 ? step / range * 100 : 0;
  }
  /** Large step as a percentage of the slider range. */
  getLargeStepPercent() {
    const { largeStep, min, max } = __privateGet(this, _props9);
    const range = max - min;
    return range > 0 ? largeStep / range * 100 : 0;
  }
  adjustPercentForAlignment(rawPercent, thumbSize, trackSize) {
    if (__privateGet(this, _props9).thumbAlignment === "center" || trackSize === 0) return rawPercent;
    const thumbHalf = thumbSize / trackSize * 100 / 2;
    const minPercent = thumbHalf;
    const maxPercent = 100 - thumbHalf;
    return minPercent + rawPercent / 100 * (maxPercent - minPercent);
  }
}, _props9 = new WeakMap(), _input2 = new WeakMap(), __publicField(_a13, "defaultProps", {
  label: "",
  step: 1,
  largeStep: 10,
  orientation: "horizontal",
  disabled: false,
  thumbAlignment: "center",
  value: 0,
  min: 0,
  max: 100
}), __publicField(_a13, "defaultInput", {
  pointerPercent: 0,
  dragPercent: 0,
  dragging: false,
  pointing: false,
  focused: false
}), _a13);

// node_modules/@videojs/core/dist/dev/core/ui/slider/slider-data-attrs.js
var SliderDataAttrs = {
  dragging: "data-dragging",
  pointing: "data-pointing",
  interactive: "data-interactive",
  orientation: "data-orientation",
  disabled: "data-disabled"
};

// node_modules/@videojs/core/dist/dev/core/ui/thumbnail/thumbnail-data-attrs.js
var ThumbnailDataAttrs = {
  loading: "data-loading",
  error: "data-error",
  hidden: "data-hidden"
};

// node_modules/@videojs/core/dist/dev/core/ui/thumbnail/thumbnail-media-fragment.js
function parseMediaFragment(text, baseURL) {
  const parts = text.trim().split("#");
  const rawURL = parts[0] ?? "";
  const hash = parts[1];
  const url = baseURL ? new URL(rawURL, baseURL).href : rawURL;
  if (!hash) return { url };
  const eqIndex = hash.indexOf("=");
  if (eqIndex === -1) return { url };
  const keys = hash.slice(0, eqIndex);
  const values = hash.slice(eqIndex + 1).split(",").map(Number);
  const data = {};
  for (let i = 0; i < keys.length; i++) {
    const key = keys[i];
    const value = values[i];
    if (key && isNumber(value) && !Number.isNaN(value)) data[key] = value;
  }
  const result = { url };
  if (isNumber(data.w)) result.width = data.w;
  if (isNumber(data.h)) result.height = data.h;
  if (isNumber(data.x) && isNumber(data.y)) result.coords = {
    x: data.x,
    y: data.y
  };
  return result;
}
function mapCuesToThumbnails(cues, baseURL) {
  const images = [];
  for (const cue of cues) {
    const fragment = parseMediaFragment(cue.text, baseURL);
    const image = {
      url: fragment.url,
      startTime: cue.startTime,
      endTime: cue.endTime
    };
    if (fragment.width) image.width = fragment.width;
    if (fragment.height) image.height = fragment.height;
    if (fragment.coords) image.coords = fragment.coords;
    images.push(image);
  }
  return images;
}

// node_modules/@videojs/utils/dist/time/format.js
var UNIT_LABELS = [
  {
    singular: "hour",
    plural: "hours"
  },
  {
    singular: "minute",
    plural: "minutes"
  },
  {
    singular: "second",
    plural: "seconds"
  }
];
function isValidTime(value) {
  return isNumber(value) && Number.isFinite(value);
}
function toTimeUnitPhrase(value, unitIndex) {
  var _a17, _b;
  return `${value} ${value === 1 ? (_a17 = UNIT_LABELS[unitIndex]) == null ? void 0 : _a17.singular : (_b = UNIT_LABELS[unitIndex]) == null ? void 0 : _b.plural}`;
}
function formatTime(seconds, guide) {
  if (!isValidTime(seconds)) return "0:00";
  const negative = seconds < 0;
  const positiveSeconds = Math.abs(seconds);
  const h = Math.floor(positiveSeconds / 3600);
  const m = Math.floor(positiveSeconds / 60 % 60);
  const s = Math.floor(positiveSeconds % 60);
  const guideAbs = guide ? Math.abs(guide) : 0;
  const gh = Math.floor(guideAbs / 3600);
  const gm = Math.floor(guideAbs / 60 % 60);
  const showHours = h > 0 || gh > 0;
  const padMinutes = showHours || gm >= 10;
  const hoursStr = showHours ? `${h}:` : "";
  const minutesStr = `${padMinutes && m < 10 ? "0" : ""}${m}:`;
  const secondsStr = s < 10 ? `0${s}` : `${s}`;
  return `${negative ? "-" : ""}${hoursStr}${minutesStr}${secondsStr}`;
}
function formatTimeAsPhrase(seconds) {
  if (!isValidTime(seconds)) return "";
  const negative = seconds < 0;
  const positiveSeconds = Math.abs(seconds);
  const h = Math.floor(positiveSeconds / 3600);
  const m = Math.floor(positiveSeconds / 60 % 60);
  const s = Math.floor(positiveSeconds % 60);
  if (positiveSeconds === 0) return `${toTimeUnitPhrase(0, 2)}${negative ? " remaining" : ""}`;
  return `${[
    h,
    m,
    s
  ].map((value, index) => value > 0 ? toTimeUnitPhrase(value, index) : null).filter(Boolean).join(", ")}${negative ? " remaining" : ""}`;
}
function secondsToIsoDuration(seconds) {
  if (!isValidTime(seconds)) return "PT0S";
  const positiveSeconds = Math.abs(seconds);
  const h = Math.floor(positiveSeconds / 3600);
  const m = Math.floor(positiveSeconds / 60 % 60);
  const s = Math.floor(positiveSeconds % 60);
  let duration = "PT";
  if (h > 0) duration += `${h}H`;
  if (m > 0) duration += `${m}M`;
  if (s > 0 || duration === "PT") duration += `${s}S`;
  return duration;
}

// node_modules/@videojs/core/dist/dev/core/ui/time/time-core.js
var DEFAULT_LABELS = {
  current: "Current time",
  duration: "Duration",
  remaining: "Remaining"
};
var _a14, _props10, _media9, _TimeCore_instances, getSeconds_fn, getText_fn, getPhrase_fn, getDatetime_fn;
var TimeCore = (_a14 = class {
  constructor(props) {
    __privateAdd(this, _TimeCore_instances);
    __privateAdd(this, _props10, { ..._a14.defaultProps });
    __privateAdd(this, _media9, null);
    if (props) this.setProps(props);
  }
  setProps(props) {
    __privateSet(this, _props10, defaults(props, _a14.defaultProps));
  }
  setMedia(media) {
    __privateSet(this, _media9, media);
  }
  getLabel(state) {
    const { label } = __privateGet(this, _props10);
    if (isFunction(label)) {
      const customLabel = label(state);
      if (customLabel) return customLabel;
    } else if (label) return label;
    return DEFAULT_LABELS[__privateGet(this, _props10).type];
  }
  getAttrs(state) {
    return {
      "aria-label": this.getLabel(state),
      "aria-valuetext": state.phrase
    };
  }
  getState() {
    const seconds = __privateMethod(this, _TimeCore_instances, getSeconds_fn).call(this);
    return {
      type: __privateGet(this, _props10).type,
      seconds,
      negative: __privateGet(this, _props10).type === "remaining" && seconds < 0,
      text: __privateMethod(this, _TimeCore_instances, getText_fn).call(this),
      phrase: __privateMethod(this, _TimeCore_instances, getPhrase_fn).call(this),
      datetime: __privateMethod(this, _TimeCore_instances, getDatetime_fn).call(this)
    };
  }
}, _props10 = new WeakMap(), _media9 = new WeakMap(), _TimeCore_instances = new WeakSet(), getSeconds_fn = function() {
  const media = __privateGet(this, _media9);
  const { type } = __privateGet(this, _props10);
  switch (type) {
    case "current":
      return media.currentTime;
    case "duration":
      return media.duration;
    case "remaining":
      return media.currentTime - media.duration;
    default:
      return 0;
  }
}, getText_fn = function() {
  const media = __privateGet(this, _media9);
  const seconds = __privateMethod(this, _TimeCore_instances, getSeconds_fn).call(this);
  return formatTime(Math.abs(seconds), media.duration);
}, getPhrase_fn = function() {
  const { type } = __privateGet(this, _props10);
  const seconds = __privateMethod(this, _TimeCore_instances, getSeconds_fn).call(this);
  if (type === "remaining") return formatTimeAsPhrase(seconds < 0 ? seconds : -Math.abs(seconds));
  return formatTimeAsPhrase(seconds);
}, getDatetime_fn = function() {
  const seconds = __privateMethod(this, _TimeCore_instances, getSeconds_fn).call(this);
  return secondsToIsoDuration(Math.abs(seconds));
}, __publicField(_a14, "defaultProps", {
  type: "current",
  negativeSign: "-",
  label: ""
}), _a14);

// node_modules/@videojs/core/dist/dev/core/ui/time/time-data-attrs.js
var TimeDataAttrs = { type: "data-type" };

// node_modules/@videojs/core/dist/dev/core/ui/time-slider/time-slider-core.js
var _a15, _props11, _media10;
var TimeSliderCore = (_a15 = class extends SliderCore {
  constructor(props) {
    super();
    __privateAdd(this, _props11, { ..._a15.defaultProps });
    __privateAdd(this, _media10, null);
    if (props) this.setProps(props);
  }
  setProps(props) {
    __privateSet(this, _props11, defaults(props, _a15.defaultProps));
    super.setProps({
      ...props,
      min: 0
    });
  }
  setMedia(media) {
    __privateSet(this, _media10, media);
  }
  getState() {
    const { duration, currentTime, seeking, buffered } = __privateGet(this, _media10);
    const { dragging, dragPercent } = this.input;
    super.setProps({
      ...__privateGet(this, _props11),
      min: 0,
      max: duration
    });
    const value = dragging ? clamp(dragPercent / 100 * duration, 0, duration) : currentTime;
    const base = super.getSliderState(value);
    const bufferedEnd = buffered.length > 0 ? buffered[buffered.length - 1][1] : 0;
    const bufferPercent = duration > 0 ? bufferedEnd / duration * 100 : 0;
    return {
      ...base,
      currentTime,
      duration,
      seeking,
      bufferPercent
    };
  }
  getLabel(state) {
    return super.getLabel(state) || "Seek";
  }
  getAttrs(state) {
    const base = super.getAttrs(state);
    const currentPhrase = formatTimeAsPhrase(state.value);
    const durationPhrase = formatTimeAsPhrase(state.duration);
    const valuetext = durationPhrase ? `${currentPhrase} of ${durationPhrase}` : currentPhrase;
    return {
      ...base,
      "aria-valuetext": valuetext
    };
  }
}, _props11 = new WeakMap(), _media10 = new WeakMap(), __publicField(_a15, "defaultProps", {
  ...SliderCore.defaultProps,
  label: "Seek",
  commitThrottle: 100
}), _a15);

// node_modules/@videojs/core/dist/dev/core/ui/time-slider/time-slider-data-attrs.js
var TimeSliderDataAttrs = {
  ...SliderDataAttrs,
  seeking: "data-seeking"
};

// node_modules/@videojs/core/dist/dev/core/ui/volume-slider/volume-slider-core.js
var _a16, _media11;
var VolumeSliderCore = (_a16 = class extends SliderCore {
  constructor(props) {
    super();
    __privateAdd(this, _media11, null);
    if (props) this.setProps(props);
  }
  setProps(props) {
    super.setProps(defaults(props, _a16.defaultProps));
  }
  setMedia(media) {
    __privateSet(this, _media11, media);
  }
  getState() {
    const { volume, muted } = __privateGet(this, _media11);
    const { dragging, dragPercent } = this.input;
    const volumePercent = volume * 100;
    const value = dragging ? this.valueFromPercent(dragPercent) : volumePercent;
    const base = super.getSliderState(value);
    return {
      ...base,
      fillPercent: muted ? 0 : base.fillPercent,
      volume,
      muted
    };
  }
  getLabel(state) {
    return super.getLabel(state) || "Volume";
  }
  getAttrs(state) {
    const base = super.getAttrs(state);
    const valuetext = `${Math.round(state.value)} percent${state.muted ? ", muted" : ""}`;
    return {
      ...base,
      "aria-valuetext": valuetext
    };
  }
}, _media11 = new WeakMap(), __publicField(_a16, "defaultProps", {
  ...SliderCore.defaultProps,
  label: "Volume"
}), _a16);

// node_modules/@videojs/core/dist/dev/core/ui/volume-slider/volume-slider-data-attrs.js
var VolumeSliderDataAttrs = { ...SliderDataAttrs };

// node_modules/@videojs/react/dist/dev/ui/buffering-indicator/buffering-indicator.js
var BufferingIndicator = (0, import_react6.forwardRef)(function BufferingIndicator2(componentProps, forwardedRef) {
  const { render, className, style, delay, ...elementProps } = componentProps;
  const playback = usePlayer(selectPlayback);
  const [core] = (0, import_react6.useState)(() => new BufferingIndicatorCore());
  core.setProps({ delay });
  if (playback) core.update(playback);
  const state = (0, import_react6.useSyncExternalStore)((cb) => core.state.subscribe(cb), () => core.state.current, () => core.state.current);
  if (!playback) {
    logMissingFeature("BufferingIndicator", "playback");
    return null;
  }
  return renderElement("div", {
    render,
    className,
    style
  }, {
    state,
    stateAttrMap: BufferingIndicatorDataAttrs,
    ref: [forwardedRef],
    props: [elementProps]
  });
});

// node_modules/@videojs/react/dist/dev/ui/hooks/use-button.js
var import_react7 = __toESM(require_react(), 1);
function useButton(params) {
  const { displayName, onActivate, isDisabled } = params;
  const buttonRef = (0, import_react7.useCallback)((element) => {
    if (element && element.tagName !== "BUTTON") console.warn(`${displayName} should render a <button> element for accessibility`);
  }, [displayName]);
  return {
    getButtonProps: (0, import_react7.useCallback)((externalProps) => {
      return mergeProps(createButton({
        onActivate,
        isDisabled
      }), externalProps);
    }, [onActivate, isDisabled]),
    buttonRef
  };
}

// node_modules/@videojs/react/dist/dev/ui/create-media-button.js
var import_react8 = __toESM(require_react(), 1);
function createMediaButton(config) {
  const { displayName, core: CoreClass, stateAttrMap, selector, action } = config;
  const corePropKeys = new Set(Object.keys(CoreClass.defaultProps));
  const Component = (0, import_react8.forwardRef)(function MediaButton(componentProps, forwardedRef) {
    const { render, className, style, ...rest } = componentProps;
    const coreProps = {};
    const elementProps = {};
    for (const key of Object.keys(rest)) if (corePropKeys.has(key)) coreProps[key] = rest[key];
    else elementProps[key] = rest[key];
    const feature = usePlayer(selector);
    const [core] = (0, import_react8.useState)(() => new CoreClass());
    core.setProps(coreProps);
    const { getButtonProps, buttonRef } = useButton({
      displayName,
      onActivate: () => action(core, feature),
      isDisabled: () => !!coreProps.disabled || !feature
    });
    if (!feature) {
      logMissingFeature(displayName, selector.displayName ?? displayName);
      return null;
    }
    core.setMedia(feature);
    const state = core.getState();
    return renderElement("button", {
      render,
      className,
      style
    }, {
      state,
      stateAttrMap,
      ref: [forwardedRef, buttonRef],
      props: [
        core.getAttrs(state),
        elementProps,
        getButtonProps()
      ]
    });
  });
  Component.displayName = displayName;
  return Component;
}

// node_modules/@videojs/react/dist/dev/ui/fullscreen-button/fullscreen-button.js
var FullscreenButton = createMediaButton({
  displayName: "FullscreenButton",
  core: FullscreenButtonCore,
  stateAttrMap: FullscreenButtonDataAttrs,
  selector: selectFullscreen,
  action: (core, state) => core.toggle(state)
});

// node_modules/@videojs/react/dist/dev/utils/use-force-render.js
var import_react9 = __toESM(require_react(), 1);
function useForceRender() {
  const [, forceRender] = (0, import_react9.useReducer)((c) => c + 1, 0);
  return forceRender;
}

// node_modules/@videojs/react/dist/dev/utils/use-latest-ref.js
var import_react10 = __toESM(require_react(), 1);
function useLatestRef(value) {
  const ref = (0, import_react10.useRef)(value);
  ref.current = value;
  return ref;
}

// node_modules/@videojs/react/dist/dev/ui/hooks/use-slider.js
var import_react12 = __toESM(require_react(), 1);
function useSlider(options) {
  const optionsRef = useLatestRef(options);
  const rootElementRef = (0, import_react12.useRef)(null);
  const thumbElementRef = (0, import_react12.useRef)(null);
  const forceRender = useForceRender();
  const [slider] = (0, import_react12.useState)(() => {
    return createSlider({
      getElement: () => rootElementRef.current,
      getThumbElement: () => thumbElementRef.current,
      getOrientation: () => optionsRef.current.orientation ?? "horizontal",
      isRTL: () => rootElementRef.current ? isRTL(rootElementRef.current) : false,
      isDisabled: () => optionsRef.current.disabled ?? false,
      getPercent: () => optionsRef.current.getPercent(),
      getStepPercent: () => optionsRef.current.getStepPercent(),
      getLargeStepPercent: () => optionsRef.current.getLargeStepPercent(),
      commitThrottle: optionsRef.current.commitThrottle,
      onValueChange: (percent) => {
        var _a17, _b;
        return (_b = (_a17 = optionsRef.current).onValueChange) == null ? void 0 : _b.call(_a17, percent);
      },
      onValueCommit: (percent) => {
        var _a17, _b;
        return (_b = (_a17 = optionsRef.current).onValueCommit) == null ? void 0 : _b.call(_a17, percent);
      },
      onDragStart: () => {
        var _a17, _b;
        return (_b = (_a17 = optionsRef.current).onDragStart) == null ? void 0 : _b.call(_a17);
      },
      onDragEnd: () => {
        var _a17, _b;
        return (_b = (_a17 = optionsRef.current).onDragEnd) == null ? void 0 : _b.call(_a17);
      }
    });
  });
  (0, import_react12.useEffect)(() => () => slider.destroy(), [slider]);
  const input = useSnapshot(slider.input);
  const state = options.computeState(input);
  (0, import_react12.useLayoutEffect)(() => {
    if (state.thumbAlignment === "edge" && rootElementRef.current && thumbElementRef.current) forceRender();
  }, [state.thumbAlignment]);
  const rootEl = rootElementRef.current;
  const thumbEl = thumbElementRef.current;
  let cssState = state;
  if (state.thumbAlignment === "edge" && rootEl && thumbEl && options.adjustPercent) {
    const isHorizontal = state.orientation === "horizontal";
    const thumbSize = isHorizontal ? thumbEl.offsetWidth : thumbEl.offsetHeight;
    const trackSize = isHorizontal ? rootEl.offsetWidth : rootEl.offsetHeight;
    cssState = {
      ...state,
      fillPercent: options.adjustPercent(state.fillPercent, thumbSize, trackSize),
      pointerPercent: options.adjustPercent(state.pointerPercent, thumbSize, trackSize)
    };
  }
  return {
    state,
    cssVars: options.getCSSVars(cssState),
    rootRef: (0, import_react12.useCallback)((element) => {
      rootElementRef.current = element;
    }, []),
    thumbRef: (0, import_react12.useCallback)((element) => {
      thumbElementRef.current = element;
    }, []),
    rootProps: slider.rootProps,
    thumbProps: slider.thumbProps
  };
}

// node_modules/@videojs/react/dist/dev/ui/mute-button/mute-button.js
var MuteButton = createMediaButton({
  displayName: "MuteButton",
  core: MuteButtonCore,
  stateAttrMap: MuteButtonDataAttrs,
  selector: selectVolume,
  action: (core, state) => core.toggle(state)
});

// node_modules/@videojs/react/dist/dev/ui/pip-button/pip-button.js
var PiPButton = createMediaButton({
  displayName: "PiPButton",
  core: PiPButtonCore,
  stateAttrMap: PiPButtonDataAttrs,
  selector: selectPiP,
  action: (core, state) => core.toggle(state)
});

// node_modules/@videojs/react/dist/dev/ui/play-button/play-button.js
var PlayButton = createMediaButton({
  displayName: "PlayButton",
  core: PlayButtonCore,
  stateAttrMap: PlayButtonDataAttrs,
  selector: selectPlayback,
  action: (core, state) => core.toggle(state)
});

// node_modules/@videojs/react/dist/dev/ui/playback-rate-button/playback-rate-button.js
var PlaybackRateButton = createMediaButton({
  displayName: "PlaybackRateButton",
  core: PlaybackRateButtonCore,
  stateAttrMap: PlaybackRateButtonDataAttrs,
  selector: selectPlaybackRate,
  action: (core, state) => core.cycle(state)
});

// node_modules/@videojs/react/dist/dev/ui/popover/context.js
var import_react13 = __toESM(require_react(), 1);
var PopoverContext = (0, import_react13.createContext)(null);
var PopoverContextProvider = PopoverContext.Provider;
function usePopoverContext() {
  const ctx = (0, import_react13.useContext)(PopoverContext);
  if (!ctx) throw new Error("Popover compound components must be used within a Popover.Root");
  return ctx;
}

// node_modules/@videojs/react/dist/dev/ui/seek-button/seek-button.js
var SeekButton = createMediaButton({
  displayName: "SeekButton",
  core: SeekButtonCore,
  stateAttrMap: SeekButtonDataAttrs,
  selector: selectTime,
  action: (core, state) => core.seek(state)
});

// node_modules/@videojs/react/dist/dev/ui/controls/context.js
var import_react14 = __toESM(require_react(), 1);
var ControlsContext = (0, import_react14.createContext)(null);
var ControlsContextProvider = ControlsContext.Provider;
function useControlsContext() {
  const ctx = (0, import_react14.useContext)(ControlsContext);
  if (!ctx) throw new Error("Controls compound components must be used within a Controls.Root");
  return ctx;
}

// node_modules/@videojs/react/dist/dev/ui/controls/controls-root.js
var import_react15 = __toESM(require_react(), 1);
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var ControlsRoot = (0, import_react15.forwardRef)(function ControlsRoot2(componentProps, forwardedRef) {
  const { render, className, style, children, ...elementProps } = componentProps;
  const controls = usePlayer(selectControls);
  const [core] = (0, import_react15.useState)(() => new ControlsCore());
  if (!controls) {
    logMissingFeature("Controls.Root", "controls");
    return null;
  }
  core.setMedia(controls);
  const state = core.getState();
  return (0, import_jsx_runtime2.jsx)(ControlsContextProvider, {
    value: {
      state,
      stateAttrMap: ControlsDataAttrs
    },
    children: renderElement("div", {
      render,
      className,
      style
    }, {
      state,
      stateAttrMap: ControlsDataAttrs,
      ref: [forwardedRef],
      props: [{ children }, elementProps]
    })
  });
});

// node_modules/@videojs/react/dist/dev/ui/popover/popover-popup.js
var import_react16 = __toESM(require_react(), 1);
var POPOVER_RESET = {
  position: "fixed",
  inset: "auto",
  margin: 0
};
var PopoverPopup = (0, import_react16.forwardRef)(function PopoverPopup2({ render, className, style, ...elementProps }, forwardedRef) {
  const { core, popover, state, stateAttrMap, anchorName, popupId } = usePopoverContext();
  const internalRef = (0, import_react16.useRef)(null);
  const composedRef = useComposedRefs(forwardedRef, (0, import_react16.useCallback)((el) => {
    popover.setPopupElement(el);
  }, [popover]), internalRef);
  const posOpts = (0, import_react16.useMemo)(() => ({
    side: state.side,
    align: state.align
  }), [state.side, state.align]);
  const anchorStyle = (0, import_react16.useMemo)(() => {
    if (!supportsAnchorPositioning()) return null;
    return getAnchorPositionStyle(anchorName, posOpts);
  }, [anchorName, posOpts]);
  const [manualStyle, setManualStyle] = (0, import_react16.useState)(null);
  (0, import_react16.useLayoutEffect)(() => {
    if (supportsAnchorPositioning()) return;
    if (!state.open) {
      setManualStyle(null);
      return;
    }
    function measure() {
      const triggerEl = popover.triggerElement;
      const popupEl = internalRef.current;
      if (!triggerEl || !popupEl) return;
      setManualStyle(getAnchorPositionStyle(anchorName, posOpts, triggerEl.getBoundingClientRect(), popupEl.getBoundingClientRect(), document.documentElement.getBoundingClientRect(), resolveOffsets(popupEl)));
    }
    measure();
    let rafId = 0;
    function reposition() {
      cancelAnimationFrame(rafId);
      rafId = requestAnimationFrame(measure);
    }
    window.addEventListener("scroll", reposition, {
      capture: true,
      passive: true
    });
    window.addEventListener("resize", reposition);
    return () => {
      cancelAnimationFrame(rafId);
      window.removeEventListener("scroll", reposition, true);
      window.removeEventListener("resize", reposition);
    };
  }, [
    state.open,
    anchorName,
    posOpts,
    popover
  ]);
  const positioningStyle = anchorStyle ?? manualStyle ?? POPOVER_RESET;
  if (!state.open) return null;
  const { onFocusOut, ...restPopupProps } = popover.popupProps;
  return renderElement("div", {
    render,
    className,
    style
  }, {
    state,
    stateAttrMap,
    ref: composedRef,
    props: [
      {
        id: popupId,
        style: positioningStyle,
        ...core.getPopupAttrs(state)
      },
      {
        ...restPopupProps,
        onBlur: onFocusOut
      },
      elementProps
    ]
  });
});

// node_modules/@videojs/react/dist/dev/utils/use-safe-id.js
var import_react17 = __toESM(require_react(), 1);
var UNSAFE_CHARS = /[^a-zA-Z0-9_-]/g;
function useSafeId(prefix) {
  const raw = (0, import_react17.useId)().replace(UNSAFE_CHARS, "");
  return prefix ? `${prefix}${raw}` : raw;
}

// node_modules/@videojs/react/dist/dev/ui/popover/popover-root.js
var import_react19 = __toESM(require_react(), 1);
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
function PopoverRoot({ open: controlledOpen, defaultOpen = PopoverCore.defaultProps.defaultOpen, onOpenChange: onOpenChangeProp, onOpenChangeComplete: onOpenChangeCompleteProp, openOnHover = PopoverCore.defaultProps.openOnHover, delay = PopoverCore.defaultProps.delay, closeDelay = PopoverCore.defaultProps.closeDelay, children, ...coreProps }) {
  const [core] = (0, import_react19.useState)(() => new PopoverCore(coreProps));
  core.setProps(coreProps);
  const isControlled = controlledOpen !== void 0;
  const onOpenChangeRef = useLatestRef(onOpenChangeProp);
  const onOpenChangeCompleteRef = useLatestRef(onOpenChangeCompleteProp);
  const closeOnEscapeRef = useLatestRef(coreProps.closeOnEscape);
  const closeOnOutsideClickRef = useLatestRef(coreProps.closeOnOutsideClick);
  const openOnHoverRef = useLatestRef(openOnHover);
  const delayRef = useLatestRef(delay);
  const closeDelayRef = useLatestRef(closeDelay);
  const [popover] = (0, import_react19.useState)(() => {
    const instance = createPopover({
      transition: createTransition(),
      onOpenChange: (nextOpen, details) => {
        var _a17;
        (_a17 = onOpenChangeRef.current) == null ? void 0 : _a17.call(onOpenChangeRef, nextOpen, details);
      },
      onOpenChangeComplete: (nextOpen) => {
        var _a17;
        (_a17 = onOpenChangeCompleteRef.current) == null ? void 0 : _a17.call(onOpenChangeCompleteRef, nextOpen);
      },
      closeOnEscape: () => closeOnEscapeRef.current ?? PopoverCore.defaultProps.closeOnEscape,
      closeOnOutsideClick: () => closeOnOutsideClickRef.current ?? PopoverCore.defaultProps.closeOnOutsideClick,
      openOnHover: () => openOnHoverRef.current,
      delay: () => delayRef.current,
      closeDelay: () => closeDelayRef.current
    });
    if (!isControlled && defaultOpen) instance.open("click");
    return instance;
  });
  const anchorName = useSafeId();
  const popupId = useSafeId("popup-");
  (0, import_react19.useEffect)(() => {
    if (controlledOpen === void 0) return;
    const { active: inputOpen } = popover.input.current;
    if (controlledOpen === inputOpen) return;
    if (controlledOpen) popover.open("click");
    else popover.close("click");
  }, [controlledOpen, popover]);
  (0, import_react19.useEffect)(() => () => popover.destroy(), [popover]);
  const input = useSnapshot(popover.input);
  core.setInput(input);
  return (0, import_jsx_runtime3.jsx)(PopoverContextProvider, {
    value: {
      core,
      popover,
      state: core.getState(),
      stateAttrMap: PopoverDataAttrs,
      anchorName,
      popupId
    },
    children
  });
}

// node_modules/@videojs/react/dist/dev/ui/popover/popover-trigger.js
var import_react20 = __toESM(require_react(), 1);
var PopoverTrigger = (0, import_react20.forwardRef)(function PopoverTrigger2({ render, className, style, ...elementProps }, forwardedRef) {
  const { core, popover, state, stateAttrMap, anchorName, popupId } = usePopoverContext();
  const triggerRef = (0, import_react20.useCallback)((el) => {
    popover.setTriggerElement(el);
  }, [popover]);
  const anchorStyle = (0, import_react20.useMemo)(() => getAnchorNameStyle(anchorName), [anchorName]);
  const { onFocusIn, onFocusOut, ...restTriggerProps } = popover.triggerProps;
  return renderElement("button", {
    render,
    className,
    style
  }, {
    state,
    stateAttrMap,
    ref: [forwardedRef, triggerRef],
    props: [
      {
        type: "button",
        style: anchorStyle,
        ...core.getTriggerAttrs(state, popupId)
      },
      {
        ...restTriggerProps,
        onFocus: onFocusIn,
        onBlur: onFocusOut
      },
      elementProps
    ]
  });
});

// node_modules/@videojs/react/dist/dev/ui/create-context-part.js
var import_react21 = __toESM(require_react(), 1);
function createContextPart(config) {
  const { displayName, tag, useContext: useContext5, staticProps } = config;
  const Component = (0, import_react21.forwardRef)(function ContextPart(componentProps, forwardedRef) {
    const { render, className, style, ...elementProps } = componentProps;
    const context = useContext5();
    return renderElement(tag, {
      render,
      className,
      style
    }, {
      state: context.state,
      stateAttrMap: context.stateAttrMap,
      ref: forwardedRef,
      props: staticProps ? [staticProps, elementProps] : [elementProps]
    });
  });
  Component.displayName = displayName;
  return Component;
}

// node_modules/@videojs/react/dist/dev/ui/slider/context.js
var import_react22 = __toESM(require_react(), 1);
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var SliderContext = (0, import_react22.createContext)(null);
function SliderProvider({ value, children }) {
  return (0, import_jsx_runtime4.jsx)(SliderContext.Provider, {
    value,
    children
  });
}
function useSliderContext() {
  const ctx = (0, import_react22.useContext)(SliderContext);
  if (!ctx) throw new Error("Slider compound components must be used within a Slider.Root");
  return ctx;
}

// node_modules/@videojs/react/dist/dev/ui/slider/slider-buffer.js
var SliderBuffer = createContextPart({
  displayName: "SliderBuffer",
  tag: "div",
  useContext: useSliderContext
});

// node_modules/@videojs/react/dist/dev/ui/slider/slider-fill.js
var SliderFill = createContextPart({
  displayName: "SliderFill",
  tag: "div",
  useContext: useSliderContext
});

// node_modules/@videojs/react/dist/dev/ui/slider/slider-thumb.js
var import_react23 = __toESM(require_react(), 1);
var SliderThumb = (0, import_react23.forwardRef)(function SliderThumb2(componentProps, forwardedRef) {
  const { render, className, style, ...elementProps } = componentProps;
  const context = useSliderContext();
  const { state, thumbRef, thumbProps, getAttrs } = context;
  const attrs = getAttrs(state);
  return renderElement("div", {
    render,
    className,
    style
  }, {
    state,
    stateAttrMap: context.stateAttrMap,
    ref: [forwardedRef, thumbRef],
    props: [
      attrs,
      thumbProps,
      elementProps
    ]
  });
});

// node_modules/@videojs/react/dist/dev/ui/slider/slider-track.js
var SliderTrack = createContextPart({
  displayName: "SliderTrack",
  tag: "div",
  useContext: useSliderContext
});

// node_modules/@videojs/react/dist/dev/ui/time/time-group.js
var import_react24 = __toESM(require_react(), 1);
var Group = (0, import_react24.forwardRef)(function Group2(componentProps, forwardedRef) {
  const { render, className, style, children, ...elementProps } = componentProps;
  return renderElement("span", {
    render,
    className,
    style
  }, {
    state: {},
    ref: [forwardedRef],
    props: [{ children }, elementProps]
  });
});

// node_modules/@videojs/react/dist/dev/ui/time/time-separator.js
var import_react25 = __toESM(require_react(), 1);
var Separator = (0, import_react25.forwardRef)(function Separator2(componentProps, forwardedRef) {
  const { render, className, style, children = "/", ...elementProps } = componentProps;
  return renderElement("span", {
    render,
    className,
    style
  }, {
    state: {},
    ref: [forwardedRef],
    props: [{
      "aria-hidden": "true",
      children
    }, elementProps]
  });
});

// node_modules/@videojs/react/dist/dev/ui/time/time-value.js
var import_react26 = __toESM(require_react(), 1);
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
var Value = (0, import_react26.forwardRef)(function Value2(componentProps, forwardedRef) {
  const { render, className, style, type, negativeSign, label, ...elementProps } = componentProps;
  const time = usePlayer(selectTime);
  const [core] = (0, import_react26.useState)(() => new TimeCore());
  core.setProps({
    type,
    negativeSign,
    label
  });
  if (!time) {
    logMissingFeature("Time.Value", "time");
    return null;
  }
  core.setMedia(time);
  const state = core.getState();
  const content = state.negative ? (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, { children: [(0, import_jsx_runtime5.jsx)("span", {
    "aria-hidden": "true",
    children: negativeSign ?? TimeCore.defaultProps.negativeSign
  }), state.text] }) : state.text;
  return renderElement("time", {
    render,
    className,
    style
  }, {
    state,
    stateAttrMap: TimeDataAttrs,
    ref: [forwardedRef],
    props: [{
      dateTime: state.datetime,
      children: content,
      ...core.getAttrs(state)
    }, elementProps]
  });
});

// node_modules/@videojs/react/dist/dev/ui/time-slider/time-slider-root.js
var import_react27 = __toESM(require_react(), 1);
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
var noopSeek = () => Promise.resolve(0);
var TimeSliderRoot = (0, import_react27.forwardRef)(function TimeSliderRoot2(componentProps, forwardedRef) {
  const { render, className, style, label, commitThrottle = TimeSliderCore.defaultProps.commitThrottle, step = TimeSliderCore.defaultProps.step, largeStep = TimeSliderCore.defaultProps.largeStep, orientation, disabled, thumbAlignment, onDragStart, onDragEnd, ...elementProps } = componentProps;
  const time = usePlayer(selectTime);
  const buffer = usePlayer(selectBuffer);
  const [core] = (0, import_react27.useState)(() => new TimeSliderCore());
  core.setProps({
    label,
    step,
    largeStep,
    orientation,
    disabled,
    thumbAlignment
  });
  const mediaRef = useLatestRef(time && buffer ? {
    ...time,
    ...buffer
  } : null);
  const duration = (time == null ? void 0 : time.duration) ?? 0;
  const { state, cssVars, rootRef, thumbRef, rootProps, thumbProps } = useSlider({
    computeState: (input) => {
      core.setInput(input);
      if (!time || !buffer) core.setMedia({
        currentTime: 0,
        duration: 0,
        seeking: false,
        seek: noopSeek,
        buffered: [],
        seekable: []
      });
      else core.setMedia({
        ...time,
        ...buffer
      });
      return core.getState();
    },
    getPercent: () => duration > 0 ? ((time == null ? void 0 : time.currentTime) ?? 0) / duration * 100 : 0,
    getStepPercent: () => core.getStepPercent(),
    getLargeStepPercent: () => core.getLargeStepPercent(),
    orientation,
    disabled,
    commitThrottle,
    adjustPercent: (rawPercent, thumbSize, trackSize) => core.adjustPercentForAlignment(rawPercent, thumbSize, trackSize),
    getCSSVars: getTimeSliderCSSVars,
    onValueCommit: (percent) => {
      const media = mediaRef.current;
      if (media) media.seek(core.valueFromPercent(percent));
    },
    onDragStart,
    onDragEnd
  });
  if (!time) {
    logMissingFeature("TimeSlider", "time");
    return null;
  }
  return (0, import_jsx_runtime6.jsx)(SliderProvider, {
    value: {
      state,
      pointerValue: core.valueFromPercent(state.pointerPercent),
      thumbRef,
      thumbProps,
      stateAttrMap: TimeSliderDataAttrs,
      getAttrs: (sliderState) => core.getAttrs(sliderState),
      formatValue: (value) => formatTime(value, duration)
    },
    children: renderElement("div", {
      render,
      className,
      style
    }, {
      state,
      stateAttrMap: TimeSliderDataAttrs,
      ref: [forwardedRef, rootRef],
      props: [
        { style: cssVars },
        rootProps,
        elementProps
      ]
    })
  });
});

// node_modules/@videojs/react/dist/dev/ui/volume-slider/volume-slider-root.js
var import_react28 = __toESM(require_react(), 1);
var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
var noopVolume = {
  volume: 0,
  muted: false,
  volumeAvailability: "unsupported",
  changeVolume: () => 0,
  toggleMute: () => false
};
var VolumeSliderRoot = (0, import_react28.forwardRef)(function VolumeSliderRoot2(componentProps, forwardedRef) {
  const { render, className, style, label, orientation, step = VolumeSliderCore.defaultProps.step, largeStep = VolumeSliderCore.defaultProps.largeStep, disabled, thumbAlignment, onDragStart, onDragEnd, ...elementProps } = componentProps;
  const volume = usePlayer(selectVolume);
  const [core] = (0, import_react28.useState)(() => new VolumeSliderCore());
  core.setProps({
    label,
    orientation,
    step,
    largeStep,
    disabled,
    thumbAlignment
  });
  const volumeRef = useLatestRef(volume);
  const { state, cssVars, rootRef, thumbRef, rootProps, thumbProps } = useSlider({
    computeState: (input) => {
      core.setInput(input);
      core.setMedia(volume ?? noopVolume);
      return core.getState();
    },
    getPercent: () => volume ? volume.volume * 100 : 0,
    getStepPercent: () => core.getStepPercent(),
    getLargeStepPercent: () => core.getLargeStepPercent(),
    orientation,
    disabled,
    adjustPercent: (rawPercent, thumbSize, trackSize) => core.adjustPercentForAlignment(rawPercent, thumbSize, trackSize),
    getCSSVars: getSliderCSSVars,
    onValueChange: (percent) => {
      var _a17;
      (_a17 = volumeRef.current) == null ? void 0 : _a17.changeVolume(percent / 100);
    },
    onValueCommit: (percent) => {
      var _a17;
      (_a17 = volumeRef.current) == null ? void 0 : _a17.changeVolume(percent / 100);
    },
    onDragStart,
    onDragEnd
  });
  if (!volume) {
    logMissingFeature("VolumeSlider", "volume");
    return null;
  }
  return (0, import_jsx_runtime7.jsx)(SliderProvider, {
    value: {
      state,
      pointerValue: core.valueFromPercent(state.pointerPercent),
      thumbRef,
      thumbProps,
      stateAttrMap: SliderDataAttrs,
      getAttrs: (sliderState) => core.getAttrs(sliderState),
      formatValue: (value) => `${Math.round(value)}%`
    },
    children: renderElement("div", {
      render,
      className,
      style
    }, {
      state,
      stateAttrMap: SliderDataAttrs,
      ref: [forwardedRef, rootRef],
      props: [
        { style: cssVars },
        rootProps,
        elementProps
      ]
    })
  });
});

export {
  isString,
  isPlainObject,
  useComposedRefs,
  shallowEqual,
  useSelector,
  useStore,
  require_jsx_runtime,
  PlayerContextProvider,
  usePlayerContext,
  usePlayer,
  useOptionalPlayer,
  useMedia,
  useMediaRegistration,
  Container,
  combine,
  createSelector,
  createStore,
  mergeProps,
  definePlayerFeature,
  bufferFeature,
  controlsFeature,
  fullscreenFeature,
  pipFeature,
  playbackFeature,
  playbackRateFeature,
  sourceFeature,
  textTrackFeature,
  timeFeature,
  volumeFeature,
  videoFeatures,
  audioFeatures,
  backgroundFeatures,
  feature_parts_exports,
  selectBuffer,
  selectControls,
  selectFullscreen,
  selectPiP,
  selectPlayback,
  selectPlaybackRate,
  selectSource,
  selectTextTrack,
  selectTime,
  selectVolume,
  createButton,
  createPopover,
  getAnchorPositionStyle,
  getAnchorNameStyle,
  getPopoverCSSVars,
  getManualPositionStyle,
  resolveOffsets,
  getPercentFromPointerEvent,
  createSlider,
  getSliderCSSVars,
  getTimeSliderCSSVars,
  ThumbnailCore,
  createThumbnail,
  createTransition,
  applyElementProps,
  logMissingFeature,
  getStateDataAttrs,
  applyStateDataAttrs,
  renderElement,
  PosterCore,
  PosterDataAttrs,
  SliderCore,
  SliderDataAttrs,
  ThumbnailDataAttrs,
  mapCuesToThumbnails,
  BufferingIndicator,
  useControlsContext,
  ControlsRoot,
  useButton,
  FullscreenButton,
  useSlider,
  MuteButton,
  PiPButton,
  PlayButton,
  PlaybackRateButton,
  usePopoverContext,
  createContextPart,
  PopoverPopup,
  PopoverRoot,
  PopoverTrigger,
  SeekButton,
  SliderProvider,
  useSliderContext,
  SliderBuffer,
  SliderFill,
  SliderThumb,
  SliderTrack,
  Group,
  Separator,
  Value,
  TimeSliderRoot,
  VolumeSliderRoot
};
/*! Bundled license information:

react/cjs/react-jsx-runtime.development.js:
  (**
   * @license React
   * react-jsx-runtime.development.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)
*/
//# sourceMappingURL=chunk-TAK4ZGZU.js.map
